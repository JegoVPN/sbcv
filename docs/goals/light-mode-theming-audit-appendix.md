# Light-Mode Theming Audit — 对抗发现附录（无损全文）

> 来源：`scripts/workflows/light-mode-theming-audit.workflow.js`（9-agent）2026-06-10 run，HEAD `8db1dc6`。
> 用途：`docs/goals/light-mode-theming-execution.md` 的 T5（归并核销）与 T9（lightRisk 核销）逐条清单。
> 盘点层（4 段 groups/specialCases、22 noncss facts、19 infra facts）的可执行部分已全部吸收进 goal 文档正文；
> 本附录收录 46 条对抗发现全文。完整机器 JSON 在 .audit/light-mode/findings.json（gitignored，可由 workflow 重跑再生）。

## Coverage adversary（盘点完整性对抗）（9 条）

### CF1 [low] Claimed styles.css color set verified EXACT — zero misses, zero phantoms in the literal domain

Mechanical extraction over src/styles.css (hex of all lengths case-insensitive, rgb/rgba/hsl/hsla/hwb/lab/lch/oklab/oklch/color/color-mix/light-dark, transparent, currentcolor; normalized to the claimed formatting) found exactly 200 distinct values, 343 hex occurrences + 57 functional occurrences (matching seed counts), and the diff against the claimed 200-value set is empty in BOTH directions. Additional negative results: no named colors in any value position (only 'violet'/'red' inside comments at lines 1270/1763/1842; all 'field' hits in TSX are className="field"); no 4/8-digit alpha hex; no hsl/color-mix/light-dark anywhere; no color functions split across lines; every claimed value appears in real declarations, not only comments (e.g. #292b2d in a comment at :149 but declared at :152, :389, :2848, :2877); currentColor only at styles.css:1174/1179 (theme-safe); ::placeholder is explicitly colored #717c88 (in set) at :2057; the only var() fallback color is the seed-known #b5bdc8 at :2719. The four agents' inventory of styles.css itself cannot be broken. Every genuine coverage hole is OUTSIDE the literal-regex domain — see the other findings.

**Evidence:** src/styles.css — extraction script diff empty both directions; counts 343 hex / 57 func / 200 distinct; named-color value-position scan returned only comment lines 1270, 1763, 1842

### CF2 [high] Vendor React Flow stylesheet renders LIGHT-mode default colors today — invisible to any styles.css inventory and untouched by tokenization

CanvasWorkspace.tsx:1 imports @xyflow/react/dist/style.css, and <ReactFlow> is given NO colorMode prop (grep over src finds zero 'colorMode'), so the vendor's default (light) --xy-* variable table is active (the vendor's dark table sits under '.react-flow.dark' at vendor style.css:53 and is never activated). Concretely user-visible surfaces painted by vendor colors absent from the claimed set: (1) MiniMap — rendered at CanvasWorkspace.tsx:611; styles.css:1139-1141 '.sbc-minimap { background: #11161c; }' overrides ONLY the background, so minimap node rectangles still use vendor #e2e2e2 (--xy-minimap-node-background-color-default) and the viewport mask uses vendor rgba(240, 240, 240, 0.6) (--xy-minimap-mask-background-color-default); there is no .react-flow__minimap selector anywhere in styles.css. (2) Marquee selection box — selectionOnDrag is enabled in select mode (CanvasWorkspace.tsx:588), painting vendor rgba(0, 89, 220, 0.08) background + 1px dotted rgba(0, 89, 220, 0.8) border (--xy-selection-*). Tokenizing every literal in styles.css cannot theme these; the plan must either set --xy-* variables in the :root/[data-theme] token tables or drive the ReactFlow colorMode prop, or the minimap/selection will be half-themed in light mode (and is arguably mis-themed in dark today). Attribution chip is safely hidden (hideAttribution: true at CanvasWorkspace.tsx:45-47); edges, handles, connection line ARE overridden in styles.css (:1821, :1553, :1579).

**Evidence:** src/components/CanvasWorkspace.tsx:1 (import "@xyflow/react/dist/style.css"), :588 (selectionOnDrag), :611 (<MiniMap .../>); src/styles.css:1139-1141 (background-only minimap override); node_modules/@xyflow/react/dist/style.css: --xy-minimap-node-background-color-default: #e2e2e2, --xy-minimap-mask-background-color-default: rgba(240, 240, 240, 0.6), --xy-selection-background-color-default: rgba(0, 89, 220, 0.08), --xy-selection-border-default: 1px dotted rgba(0, 89, 220, 0.8), .react-flow.dark at vendor line 53

### CF3 [medium] Percent-encoded colors in the favicon data-URI (index.html) evade every hex regex; #0d1116 is absent from the claimed set entirely

index.html:31 inlines the favicon as an SVG data-URI whose colors are URL-encoded: fill="%230d1116" (dark box) and %23c7ff00 six times (brand lime stroke/dots). '%23' is '#', so no #-prefixed regex — including the one used for the claimed inventory and any future tokenization-completeness gate — will ever see these. The same #0d1116 also paints the in-app logo hexagon at SbcvLogo.tsx:22 (seed-known); the value appears in NO form in the claimed set. Why it matters: (a) the in-app logo's dark fill is a user-visible surface that stays dark on light topbar unless SbcvLogo's fill moves to a CSS class/var (its stroke already comes from styles.css:175 .sbcv-logo__hexagon); (b) the favicon will sit as a dark tile on light browser chrome — acceptable as branding or fixable with a prefers-color-scheme <style> inside the SVG data-URI; (c) any grep-based 'no raw colors remain' CI guard the plan adds must also match '%23[0-9a-f]{3,8}' or this class of color silently re-accumulates.

**Evidence:** index.html:31 (fill="%230d1116", stroke="%23c7ff00", six %23c7ff00 circle fills); src/components/SbcvLogo.tsx:22 (fill="#0d1116"); src/styles.css:175 (.sbcv-logo__hexagon)

### CF4 [medium] Off-by-one color collision: TSX #1f2730 (canvas dot grid) vs in-set #1f2731 — one digit apart, easy to falsely mark covered

The claimed set contains #1f2731 (a real styles.css value) but NOT #1f2730, which is hardcoded as the React Flow <Background color="#1f2730"> dot-grid color at CanvasWorkspace.tsx:591 (location seed-known). The two values differ by one in the last digit, so a human or fuzzy-dedupe pass auditing 'is the canvas background dot color tokenized?' can trivially confuse them. The dot grid is the single most visible canvas surface; in light mode it must flip, which requires the TSX prop to become a var()-carrying value (CSS variables are legal in SVG pattern fill via the style/color prop) or a theme-aware lookup — tokenizing styles.css alone leaves the canvas dots dark. Same class of fix needed for connectionLineStyle stroke "#e4e9ee" at CanvasWorkspace.tsx:49-54: that value IS in the set and in styles.css, but this TSX occurrence is an inline SVG style object that the styles.css token table will not retheme unless rewritten to 'var(--...)'.

**Evidence:** src/components/CanvasWorkspace.tsx:591 (<Background color="#1f2730" gap={18} size={1} />), :49-54 (connectionLineStyle stroke: "#e4e9ee"); claimed set contains #1f2731 but not #1f2730

### CF5 [medium] Scrollbars, form controls, selection highlight, and dialog backdrop have ZERO literals to tokenize — they ride entirely on color-scheme: dark (styles.css:2)

Verified absences in styles.css: no ::-webkit-scrollbar* rules, no scrollbar-color, no accent-color, no caret-color, no ::selection, no ::backdrop, no :autofill, no text-decoration-color/column-rule, and the only @media queries are three width breakpoints (:304, :2634, :3144) — no prefers-color-scheme/forced-colors/print. All of those UA-painted surfaces (scrollbars in the inspector/palette overflow panes, checkbox/radio/select chrome, text-selection highlight, native dialog backdrops) are dark today solely because of 'color-scheme: dark' at styles.css:2. A tokenization pass that only rewrites color literals will never touch this declaration, and 'color-scheme' is not a color value, so it is structurally outside the claimed inventory. The light theme MUST override color-scheme itself under [data-theme="light"] (color-scheme: light) or every scrollbar and native control stays dark on light panels — a glaring, user-visible defect. Note ::placeholder is NOT in this bucket: it is explicitly colored #717c88 at styles.css:2055-2058 and tokenizes normally.

**Evidence:** src/styles.css:2 (color-scheme: dark); grep over styles.css: zero hits for ::selection, ::-webkit-scrollbar, scrollbar-color, accent-color, caret-color, ::backdrop, :autofill; @media only at :304, :2634, :3144 (width-only)

### CF6 [medium] CodeMirror JSON viewer injects a full vendor dark palette at runtime via theme="dark" — no literals in any project file

ConfigJsonViewerDialog.tsx:72 hardcodes theme="dark" on the @uiw/react-codemirror editor (seed-known; confirmed it is the ONLY CodeMirror usage in src). The editor's background, gutter, syntax-highlight token colors, selection, and cursor are all injected as CSS-in-JS by the vendor dark theme at runtime — none of those colors exist in styles.css, TSX, or index.html, so they are unreachable by both the inventory and the tokenization rewrite. In light mode the entire export/view-JSON dialog body remains a dark editor unless the theme prop is bound to the app theme state (e.g. theme={appTheme === 'light' ? 'light' : 'dark'} or an EditorView.theme built from the semantic tokens). This is the largest single contiguous surface that tokenization cannot fix.

**Evidence:** src/components/ConfigJsonViewerDialog.tsx:72 (theme="dark"); grep -ril codemirror over src returns only ConfigJsonViewerDialog.tsx; zero CodeMirror color literals found by hex/functional scans of src

### CF7 [low] filter: brightness() hover affordances encode a dark-theme assumption with no color literal

Status-pill hovers re-assert their fill then apply 'filter: brightness(1.1)' at styles.css:521, :527, :533, and a node icon-button hover uses brightness(1.14) at :1880 (plus generic brightness transitions at :508, :1865). 'Brighten on hover' is a dark-theme idiom: on a light theme the same filter pushes already-light fills toward white, weakening or inverting the hover signal (e.g. the lime #c7ff00 valid pill at :520 brightened over a white topbar). These declarations contain no color value, so they are invisible to the color inventory by construction, and the planned :root/[data-theme] variable swap will not correct them; the light table needs either brightness(<1) overrides or a tokenized '--hover-brightness' factor.

**Evidence:** src/styles.css:519-535 (three .status-pill--*:hover rules with filter: brightness(1.1)), :1880 (brightness(1.14))

### CF8 [low] index.html has no theme-color meta and no pre-bundle background — first-paint flash inverts per theme

index.html (39 lines) contains no <meta name="theme-color">, no <meta name="color-scheme">, no inline <style>, and no background on <html>/<body>; styles.css arrives only with the JS bundle (imported at App.tsx:9). Today that means a white UA-default flash before the dark stylesheet lands. For the planned three-state theme with localStorage persistence, the standard fix (inline bootstrap script in <head> that sets data-theme before first paint) should also set an early background-color and a theme-color meta per theme, otherwise dark-preference users get a white flash and light users a dark flash on every load. Not a color-inventory miss (there are no color values to list), but it is the one in-scope index.html surface beyond the favicon and it must appear in the execution plan.

**Evidence:** index.html:1-39 (full file: no theme-color/color-scheme meta, no inline style); src/App.tsx:9 (import "./styles.css")

### CF9 [low] e2e specs assert hardcoded dark-theme computed colors — safe for the pixel-identical dark pass, a trap for any light-mode test

Playwright specs assert literal dark-theme colors: e2e/brand-hover.spec.ts:6/:26 (#171d24), :12 (rgb(41, 43, 45) = #292b2d), :13 (rgba(0, 0, 0, 0)); e2e/editor.spec.ts:71/:110 (rgb(199, 255, 0)), :73 (rgb(89, 97, 106)), :105 (rgb(45, 153, 255)); e2e/mobile.spec.ts:166-268 (rgb(45, 153, 255) x6); e2e/port-click-redesign.spec.ts:209 (rgb(255, 93, 93)), :339 (rgb(255, 123, 123)). Because the tokenization route keeps :root dark values verbatim, these all keep passing and double as a free pixel-identity regression net for the refactor. But the moment a test (or the system-follow default under a headless browser reporting prefers-color-scheme: light) runs the app in light mode, every one of these breaks; light-mode e2e must pin data-theme or assert against the token variables instead.

**Evidence:** e2e/brand-hover.spec.ts:6,12,13,26; e2e/editor.spec.ts:71,73,105,110; e2e/mobile.spec.ts:166,172,198,239,264,268; e2e/port-click-redesign.spec.ts:209,339

## Token-merge adversary（归并/拆分裁决）（19 条）

### TM1 [high] #101418 polarity clash: same hex is RESTING control fill in one component and SELECTED fill in two others — three segments gave it three different role names and all missed the selected-state use

Verified roles for #101418 (12 real uses + 2 comment mentions): global <button> resting fill (line 33), official-check-btn (758), palette mode-pill RESTING bg (924, whose ACTIVE state is #151a20 at 930), palette nav ACTIVE bg (959), doc-link (1095), search inputs (886, 1651), port circle (1384), port action chip (1482), collapsed settings-module/advanced-fields card (2079-2083), action-grid button (2243), and ACTIVE tab bg paired with #c7ff00 text/border (2336-2339). css-1 labeled it surface-raised (wrong: it is the control fill), css-2 surface-input, css-3 surface-sunken — and at 924 it means 'unselected' while at 959/2337 it means 'selected'. One token cannot serve both polarities in light mode (a light 'selected' fill needs a tint the resting buttons must not get). SPLIT into: --surface-control (33, 758, 924, 1095, 2243), --surface-input-search (886, 1651 — alias of --surface-control or --surface-input, decide once), --surface-selected (959, 2337 — pairs with --accent-brand-fg), --port-bg (1384, 1482), --surface-sunken-card (2083). All alias the same #101418 in :root for pixel-identity.

**Evidence:** src/styles.css:31-33 `button { border:1px solid #29313a; background:#101418 }`; :924 `.palette-mode-pills button { background:#101418 }` (resting) vs :927-930 `.palette-mode-pills button.is-active { background:#151a20 }`; :959 active nav `background:#101418`; :2336-2339 `.panel-tabs .is-active { background:#101418; color:#c7ff00; border-color:#c7ff00 }`; :2079-2083 `.settings-module-card { background:#101418 }`; :1384, :1482 port fills

### TM2 [high] #151a20 value collision across segments: modal dialog surface (css-3 'surface-overlay') vs active-pill fill (css-2 'surface-active') — a value-keyed union would alias a dialog to a pressed state

Both segments classified their own use correctly, but the cross-segment collision is unflagged: line 930 is `.palette-mode-pills button.is-active` background (an active-control fill) and line 2511 is `.json-viewer-dialog` background (a top-level modal surface under the scrim). In light mode the dialog goes ~white while the active pill needs a visible tint/depression — opposite directions. SPLIT: --surface-overlay-dialog (2511) vs --surface-active (930); both #151a20 in :root. Any mechanical 'same hex -> same token' pass must be run per-role, not per-value.

**Evidence:** src/styles.css:927-930 `.palette-mode-pills button.is-active { ... background:#151a20 }`; :2503-2512 `.json-viewer-dialog { ... background:#151a20; box-shadow:0 32px 90px rgba(0,0,0,0.58) }`

### TM3 [high] css-3's special-case for lines 1184/1189 is factually wrong ('color on lime buttons') — the background is #f3f4f6/#ffffff, so the correct tokens are surface-inverse/text-inverse (css-2), not text-on-accent

Read verification: `.react-flow__controls-button[data-active="true"] { background:#f3f4f6; color:#1c1e20 }` and `:hover { background:#ffffff; color:#1c1e20 }` — there is no lime anywhere in the rule. If the css-3 label (text-on-accent) won the merge, light mode would keep the ink dark while the inversion logic flips the surface dark, producing dark-on-dark. Ruling: --surface-inverse (#f3f4f6) / --surface-inverse-hover (#ffffff) / --text-inverse (#1c1e20), defined relationally so light mode swaps to dark-surface + light-text. Separately, the same #1c1e20 value is a SURFACE at 2523 (.json-viewer-header), 2771 (.mobile-topbar) and uppercase #1C1E20 at 126/328/418/1150 — the text-inverse token must never share a token with that surface family despite identical dark values.

**Evidence:** src/styles.css:1182-1190 `.react-flow__controls-button[data-active="true"] { background:#f3f4f6; color:#1c1e20 } ... :hover { background:#ffffff; color:#1c1e20 }`; surface uses at :126, :328, :418, :1150, :2523, :2771

### TM4 [high] #ff8c69 spans two STATUS families: palette 'Legacy/deprecated' tag text (warn-coral) and toast ERROR icon (danger) — plus the danger-icon tier itself is fragmented three ways

Verified: line 1075 `.palette-add--deprecated small { color:#ff8c69 }` (the 'warm quality-bar' Legacy treatment, comment at 1063-1064) vs line 3267 `.toast--error .toast__icon { color:#ff8c69 }` (an error severity icon). css-2 filed the hex under accent-warn, css-4 under accent-danger; neither saw the other, and a value-keyed tokenizer would fold them, coupling the light-mode error-icon retune (must darken toward danger red) to the Legacy tag hue (stays warm coral). SPLIT: --accent-warn-legacy-fg (1075, with border #4a2727 at 1079) vs --status-error-icon-toast (3267). Also MERGE the other two error icons #ff7777 (2889 mobile status) + #ff7b7b (1779 node delete) — 4 RGB points apart, same job — into one --accent-danger-icon, and decide whether the toast error icon re-points to it (visible hue change, needs design sign-off) or keeps its verbatim alias.

**Evidence:** src/styles.css:1074-1079 `.palette-add--deprecated small { color:#ff8c69 } .palette-add--deprecated { border-color:#4a2727 }`; :3266-3268 `.toast--error .toast__icon { color:#ff8c69 }`; :2888-2890 `color:#ff7777`; :1779 `color:#ff7b7b`

### TM5 [high] 1-2 RGB-point interleave across role boundaries: border #2a333d / border #2b343e / hover-background #2b333d (and same-hex #2a3340 border vs hover) — naive near-dup folding will merge borders with hover fills

Three values within 2 RGB points serve two different roles across segments 3 and 4: #2a333d is border-default x5 (2302, 2381, 2509, 2522, 2601), #2b343e is border-default x4 (2081, 2096, 2200, 2284), but #2b333d is the .toast__action HOVER BACKGROUND (3292). css-4 also confirmed the same-hex case: #2a3340 is a border at 552/735/757/2960/3243 but a hover background at 3311. Ruling for the tokenizer: near-duplicate clustering must run WITHIN a role group only. Merge #2a333d+#2b343e (and neighbor-slice #30363d at 850, #2f3944, #303944 at 58/1515/1645/3044) into --border-default; map 3292 and 3311 to --surface-hover-raised. Any value-first dedup script will silently produce a border token that doubles as a hover fill and break one of the two in light mode.

**Evidence:** src/styles.css:2381 `border:1px solid #2a333d` vs :3291-3293 `.toast__action:hover { background:#2b333d }` vs :2081 `border:1px solid #2b343e`; same-hex pair :3243 `border:1px solid #2a3340` vs :3311 `background:#2a3340`

### TM6 [high] Plan-level contradiction: ':root holds current dark values verbatim / pixel-identical' is incompatible with the dozens of cross-value MERGEs all four segments propose — an explicit two-phase policy is required

Every merge of non-identical values changes dark-mode pixels: e.g. raised surfaces #1a1d20 (852) / #1a1e21 (1286) / #1C1E20 (1150), the ~24-value gray text ramp -> 4 tokens, #f4f7f9 (60, 3047) into #eef2f4, border ramps, 13 shadow alphas -> 3-4. The deltas are 1-8 RGB points (mostly imperceptible) but they are not 'verbatim'. Recommend deciding ONE policy before tokenization: (A) two-phase — phase 1 mints one token/alias per verbatim value (pixel-identical, mechanically verifiable with a before/after screenshot diff), phase 2 collapses aliases as a separate reviewed change; or (B) accept-list normalization — merges allowed only within a stated per-channel delta (e.g. <=8) and within the same role group, with visual-diff sign-off. Without this ruling, reviewers will reject merge PRs as violating the stated pixel-identity constraint, or worse, the constraint silently erodes.

**Evidence:** src/styles.css:852 `.inspector { background:#1a1d20 }` vs :1150 `background:#1C1E20` vs css-2's #1a1e21 at 1286; :60 `color:#f4f7f9` vs :34 `color:#eef2f4`; shadow alphas 0.28/0.34/0.36/0.38/0.40/0.42/0.46/0.48/0.50/0.52/0.55/0.58/0.60 across :829, :853, :2512, :3244

### TM7 [medium] The four segments' text tiers do not line up — same hex lands in different tiers per segment (#9aa5b0, #e6edf3, #6f7b86); a single value-banded ramp ruling is needed

Verified collisions: #9aa5b0 = popover badge text (734) and palette mode-pill resting label (922) — css-1 says text-muted, css-2 says text-secondary; ruling: muted. #e6edf3 = react-flow controls-button color (1162), settings <summary> label (2140), mobile-validation-run (2843), mobile icon-btn (2920) — css-2/css-3 say text-primary, css-4 says text-secondary; all four are interactive-control resting text, ruling: --text-primary (or a --text-control alias of it; hover-to-#fff maps to a text-primary-strong token, inverting to 'darker' in light). #6f7b86 = popover close icon (615) and palette-group h2 (871) — css-1 says text-disabled, css-2 says text-muted; meanwhile true disabled text #707b86 (1071, .palette-add:disabled small) is 1 point away. Ruling: keep ONE dark value but two tokens (--text-muted-weak for 615/871, --text-disabled for 1071) since light-mode contrast targets differ (muted must stay readable, disabled may not). Proposed file-wide bands: primary >= ~#dcdfe4+ (#eef2f4 canonical), secondary #aab2bc-#d7dee7 (canonical #aeb8c2 or #b5bdc8), muted #85909a-#9faab5 (canonical #8a96a3), disabled #6f7b86-#73808b.

**Evidence:** src/styles.css:734 `color:#9aa5b0` (badge) and :922 `color:#9aa5b0` (pill label); :1162, :2140, :2843, :2920 all `color:#e6edf3` on controls; :615 and :871 `color:#6f7b86` vs :1069-1071 `.palette-add:disabled small { color:#707b86 }`

### TM8 [medium] Undefined var --color-text-muted (styles.css:2719): its fallback #b5bdc8 belongs to the SECONDARY band by value, not muted — defining the missing variable with the merged muted value would visibly darken the control

#b5bdc8 (181,189,200) sits with the secondary cluster #aeb8c2 (857: .panel-title; 1385: port label) / #b3bcc6 (3037) / #b7c2cd (643), ~43 RGB points lighter than the muted canonical #8a96a3 (138,150,163). Two failure modes for the tokenizer: (1) 'define the missing var' as the merged muted value -> .field__reveal-button idle text darkens noticeably, breaking pixel-identity; (2) define --color-text-muted: #b5bdc8 -> a token NAMED muted carries a secondary-tier value and poisons every future use. Ruling: delete the var() reference at 2719, point the reveal-button at --text-secondary (dark value #b5bdc8 or the merged secondary canonical per the phase policy), and never ship a token named --color-text-muted (the new taxonomy uses --text-muted).

**Evidence:** src/styles.css:2719 `color: var(--color-text-muted, #b5bdc8)`; secondary-band anchors :857 `.panel-title { color:#aeb8c2 }`, :1385 `color:#aeb8c2`; muted anchor #8a96a3 x9 file-wide (e.g. :606)

### TM9 [medium] Per-severity status ICON colors are inconsistent across surfaces and no segment minted the matrix: error = #e34b4b | #ff7777 | #ff7b7b; info = #59b4ff | #8aa0b4; checking = #aeb9c6 | #d7dee7

Verified: diagnostics popover severity icons are #e34b4b/#f2bc4b/#59b4ff (697-707); mobile validation status icons are #c7ff00/#f2bc4b/#ff7777/#aeb9c6 (2880-2894); toast icons are #6fcf86/#ff8c69/#8aa0b4 (3262-3271); node delete affordance is #ff7b7b (1779). Light mode requires retuning every one of these (all are bright-on-dark), and without a per-severity token matrix the retune happens 8+ times with drift. Ruling: mint --status-{valid,warn,error,info,checking}-icon as the semantic layer; per-surface verbatim aliases (e.g. --status-error-icon-toast: #ff8c69) preserve dark pixels; light mode overrides the 5 base tokens once. This also makes the desktop/mobile divergence (error #e34b4b vs #ff7777) an explicit recorded decision instead of an accident.

**Evidence:** src/styles.css:697-707 popover icons (#e34b4b, #f2bc4b, #59b4ff); :2880-2894 mobile icons (#c7ff00, #f2bc4b, #ff7777, #aeb9c6); :3262-3271 toast icons (#6fcf86, #ff8c69, #8aa0b4); :1779 `color:#ff7b7b`

### TM10 [medium] Status-checking (neutral) family is scattered across three segments with no family token, and its fill #20262d is shared with two HOVER fills — split required

The 4th validation state has four parts in three slices: fill #20262d (482, .status-pill--checking), text #d7dee7 (481), mobile icon #aeb9c6 (2893), mobile border rgba(137,151,168,0.36) (2829 per css-4). css-1 filed fill under surface-raised and text under generic text-secondary (where #d7dee7 also serves real secondary text at 629/648/759); css-4 minted misc-status-neutral. Meanwhile #20262d is ALSO the chip-picker item hover (1713) and mobile-row-button hover (3083). Ruling: mint --status-checking-{bg,fg,icon,border} (aliases: bg=#20262d, fg=#d7dee7) symmetric with the valid/warn/error trios, and map 1713/3083 to --surface-hover-raised. Without the split, a light-mode hover retune silently recolors the checking pill.

**Evidence:** src/styles.css:480-483 `.topbar-actions .status-pill--checking { color:#d7dee7; background:#20262d }`; :1710-1714 `.chip-picker__item:hover { background:#20262d }`; :3082-3084 `.mobile-row-button:hover { background:#20262d }`; :2892-2894 checking icon `#aeb9c6`; secondary uses of #d7dee7 at :629, :648, :759

### TM11 [medium] MERGE the three light-blue foregrounds #59b4ff / #65b0ff / #6fb8ff into one --accent-info-fg; keep platform #6fb6e6, banner #9fc6ff, and toast #8aa0b4 separate

Verified contexts: #59b4ff = popover info severity icon (706), #65b0ff = input-port icon (1390), #6fb8ff = palette table/open tag text (1048, 1083). All three are 'light blue fg on dark' within ~22 RGB points, in the same info/blue family; no segment proposed the cross-segment merge because each saw only one or two. Ruling: one --accent-info-fg (canonical #6fb8ff, the most-used), with a --port-input-fg alias for 1390 so the canvas can detune independently if the light canvas needs it. Explicitly EXCLUDE: #6fb6e6 (1267, platform badge — comment at 1264 wants it distinct), #9fc6ff (2700, channel banner text, belongs to the banner trio with its unrelated rgba bases 38,87,158 / 70,132,215 which cannot derive from #2d99ff), #8aa0b4 (3271, toast info icon — grayer, belongs to the status-icon matrix), and #2d99ff selection core (1298/1303/1322/1830) which stays --accent-info-core/edge-highlighted.

**Evidence:** src/styles.css:705-707 `.diagnostics-popover__item--info .diagnostics-popover__icon { color:#59b4ff }`; :1390 `color:#65b0ff`; :1048, :1083 `color:#6fb8ff`; :1266-1267 platform `background:#102a3a; color:#6fb6e6`; :2700 `color:#9fc6ff`; :3271 `color:#8aa0b4`

### TM12 [medium] Unproposed cross-segment danger-dark merges: borders #5a2a30/#5a2b2b/#5a2f33 -> one token; deep surfaces #2a1416/#1c1416 -> one token; strong pair #712d34+#a9444e already consistent

Each segment minted its own danger darks; nobody could see the trio. Verified: #5a2a30 = node-delete resting border (1777), #5a2b2b = lazy-layer-error border (2111), #5a2f33 = toast error border (3255) — all 1px borders on danger-tinted dark surfaces, max delta 5 RGB points -> MERGE to --accent-danger-border-subtle. #2a1416 = node-delete resting bg (1778) and #1c1416 = lazy-layer-error bg (2113) — both 'near-black with red undertone' surfaces -> MERGE to --accent-danger-surface (14-point R delta on near-black; run through the phase policy). The hover/strong pair #712d34 bg + #a9444e border (1796-1797, 1817-1818) plus #a9444e reused alone as the diagnostic error-card border (2623) -> --accent-danger-surface-strong / --accent-danger-border-strong. Also MERGE soft danger text #e79a9a (1977) + #ff9a9a (2694) -> --accent-danger-fg-soft. Keep #ff5d5d (1596, invalid wire) and #ff5470 (1845, dangling wire) as separate edge tokens per css-2.

**Evidence:** src/styles.css:1776-1779 `.sbc-node__delete { border-color:#5a2a30; background:#2a1416; color:#ff7b7b }`; :2111-2113 `border:1px solid #5a2b2b; background:#1c1416`; :3254-3256 `.toast--error { border-color:#5a2f33 }`; :1796-1797 and :1817-1818 `background:#712d34; border-color:#a9444e`; :2623 `border-color:#a9444e`

### TM13 [medium] Four segment-local names for the opaque control surface (surface-pill / surface-control-chip / surface-button / surface-control) hide TWO real value bands and one cross-segment 3-value merge

Band 1, warm neutral #333537: channel-select bg (431), topbar action bg (447), react-flow controls chip (1161), mobile icon-btn (2919) — but the SAME hex is a border at 326/340/2840 (css-1/css-4 split already flagged; confirmed). Band 2, blue-gray ~#252b32: node status chip #252b31 (1728), json-viewer header button #252b32 (2560), toast action #232a32 (3282) — max 3 RGB points apart across three segments, unproposed MERGE -> --surface-control (canonical #252b32). Keep #202832 separate (chip-picker icon bg at 1706 AND a border at 1478 — its own same-hex split). Hover tokens: --surface-control-neutral-hover = #494B4D/#494b4d (451, 1169, 2927) and --surface-control-hover = #303842 (2564). Correction to css-4: '#494b4d is a hover on an already-light control' is wrong — the resting bg at 2919 is dark #333537; no extra hover-on-light token is needed (the only light control is the data-active inverse pair). Unify the group name as surface-control-* across all four segments.

**Evidence:** src/styles.css:431, :447, :1161, :2919 `background:#333537` vs borders :326, :340, :2840; :1728 `background:#252b31`; :2560 `background:#252b32`; :3282 `background:#232a32`; hovers :451/:1169 `#494B4D`, :2927 `#494b4d`, :2564 `background:#303842`; :2919 resting `background:#333537` (refutes 'already-light')

### TM14 [medium] #11161c carries four roles (global input, minimap, connected port, inset cards) and css-3's 'surface-raised' label for it is upside-down — the cards are DARKER than their parent panel

Verified: global input/select/textarea fill (59), .sbc-minimap (1140), .sbc-port.is-connected (1411), .reference-card (2202), .rule-card (2383), .diagnostic (2604), mobile input (3046). The three css-3 cards sit on .inspector whose background is #1a1d20 (852) — #11161c (17,22,28) is darker than #1a1d20 (26,29,32), so they are INSET/sunken cards, not 'raised'; carrying the surface-raised label into the table would invert their light-mode treatment (raised = whiter + shadow vs inset = gray fill). Ruling: --surface-input (59, 3046) with alias --surface-card-inset (2202, 2383, 2604; same dark value), plus --surface-minimap (1140) and --port-connected-bg (1411) as css-2 proposed. Note the system has TWO input fills today: #11161c (global, 59) vs #101418 (panel search inputs, 886/1651) — keep as two aliases now, decide unification in the light palette.

**Evidence:** src/styles.css:55-59 `input,select,textarea { ... background:#11161c }`; :2195-2202 `.reference-card { ... background:#11161c }` on :840-852 `.inspector { ... background:#1a1d20 }`; :2380-2383 `.rule-card`; :2600-2604 `.diagnostic`; :1140 minimap; :1411 connected port; :3046 mobile input

### TM15 [medium] 'surface-overlay' means three incompatible mechanics across the four segments: opaque hex overlays, high-alpha dark glass, and an opaque dialog — light mode needs different machinery for each

css-1's overlay = rgba(16,20,24,0.98) frosted popover (551); css-2's = four dark-glass rgba surfaces (palette 825 rgba(15,20,26,0.88)+blur, pill 1133, port label 1519, chip-picker 1638); css-3's = rgba(25,29,33,0.96) panel (2304) PLUS the opaque #151a20 dialog (2511); css-4's = three fully OPAQUE hexes (#1c1e20 2771, #14181d sheet, #1b2026 toast 3242). One label, three behaviors. Light theme: opaque overlays just lighten; glass overlays need a WHITE base at similar alpha (alpha-only flips fail, as css-2 noted); and the glass bases duplicate opaque tokens' RGB (rgba(16,20,24,*) IS #101418, the control/sunken hex) so deriving glass from the opaque token would wrongly couple them when #101418's light value becomes a light-gray control fill while the glass base must become near-white. Ruling: two token shapes — --surface-overlay-* (opaque hex: popover/sheet/toast/dialog) and --surface-glass-* stored as an --rgb triplet + alpha (or color-mix) so each theme sets its own base; never derive a glass base from an opaque surface token.

**Evidence:** src/styles.css:816-830 `.palette { background:rgba(15,20,26,0.88); ... backdrop-filter:blur(14px) }`; :551 rgba(16,20,24,0.98) (= #101418 base, cf. :33); :2511 opaque dialog `#151a20`; :3242 opaque toast `#1b2026`; :2771 opaque mobile topbar `#1c1e20` under inert blur

### TM16 [medium] Brand lime #c7ff00 is the table's god-token: 39 uses spanning at least 9 sub-roles; minimum viable decomposition is 4-5 tokens plus one rgb-triplet for the 6-step alpha ladder

Confirmed sub-roles: status-pill FILL with #151900 ink (470-472, re-asserted 519-522), active-control TEXT+BORDER (927-929, 2336-2339), focus outlines (1197-1199; also 156 per css-1), severity/valid icons (1707, 1729, 2880-2881, 3090), edge strokes and port states (css-2: 1573-1587, 1822, 1856-1881), logo artwork (176-193), toast action TEXT on #232a32 (3282-3283), solid edge-remove FILL (1862-1863 per css-2), and on-tint text #f6ffe8 over rgba(199,255,0,0.09) (1037-1040). Lime-as-text/outline fails contrast on light surfaces while lime-as-fill survives; #7ca300 already exists as the dimmed 'success' lime (2631) and is the natural seed for the light --accent-brand-fg. Ruling: --accent-brand-fill (+ --text-on-brand #151900), --accent-brand-fg (text/border/icon; light value ~#5f7a00-#7ca300), --accent-brand-focus (focus rings, must pass on BOTH themes), --accent-brand-edge (canvas wire, tuned against the canvas token), and --accent-brand-rgb to derive the alpha ladder 0/.09/.12/.34/.5/.86 via color-mix. Resist per-use tokens (would mint 10+) and resist keeping one token (light mode then breaks either fills or text).

**Evidence:** src/styles.css:470-472 fill+ink; :927-930 and :2336-2339 text/border on dark; :1197-1199 `outline:2px solid #c7ff00`; :3282-3283 `.toast__action { background:#232a32; color:#c7ff00 }`; :1037-1040 `.palette-add.is-added { border-color:#c7ff00; background:rgba(199,255,0,0.09); color:#f6ffe8 }`; :2631 `border-color:#7ca300`

### TM17 [low] Final token-table estimate: ~85-100 semantic tokens (about 75 value-distinct + component aliases) IF alpha literals are derived from ~10 rgb-triplets; the god-groups that need active flattening are hover fills (11 values), gray text (~24 values), borders (~16), shadows (13 alphas)

Breakdown from the unified taxonomy: surfaces ~26 (app, canvas+2 dot layers, minimap, raised, 4 overlay/glass, dialog, sheet, toast, input x2, control x2+chip, selected, active, 3-4 hover tiers, inverse pair, code, skeleton pair, grabber, port x2, inset-card alias), text ~10 (4 ramp tiers + placeholder/inverse + 4 on-accent inks #151900/#1f1600/#fff/#f6ffe8), borders ~7 (subtle/default/strong/hover/focus/input-transparent/divider), brand ~6, info ~7, danger ~10, warn+legacy ~6, gated 3, platform 2, banner trios 12 (4 variants x bg/border/fg — collapsible to ~6 if derived from accent rgb-triplets), success-green 2, status-checking 4, status-icon matrix 5, edges 3 (neutral #e4e9ee + invalid + dangling; default/highlighted alias brand/info), shadow 3-4 elevations, scrim 2, ink-ghost 3, inset-highlight 1, logo 2-3 (incl. SbcvLogo.tsx #0d1116 badge fill and #59616a idle stroke). Counting every alpha literal as its own token instead would exceed 140 and make the light override table unmaintainable. Hover fills to collapse: #171d24(47,1109), #151b22, #1a222b(770), #20262d(1713,3083), #252d36, #232629, #292b2d(2848,2877), #2b333d(3292), #2a3340(3311), #303842(2564), #494b4d(451,1169,2927) -> 3-4 tokens (hover-on-control, hover-on-raised, hover-on-neutral, hover-strong).

**Evidence:** Counts verified by grep over src/styles.css (3313 lines; e.g. hover fills at :47, :770, :1713, :2564, :2848, :2877, :3083, :3292, :3311; 13 distinct black shadow alphas; banner trios at :2679-2700) plus TSX consumers CanvasWorkspace.tsx:50/:591, SbcvLogo.tsx:22, ConfigJsonViewerDialog.tsx:72

### TM18 [low] Canvas has TWO stacked dot systems with different literals — CSS radial grid and React Flow <Background> — and the RF dot color #1f2730 must not merge with the 1-point-away border #1f2731

Verified: .canvas-shell paints its own dot grid `radial-gradient(circle at 1px 1px, rgba(147,161,176,0.13) 1px, transparent 0), #080b10` (1118-1121) while CanvasWorkspace.tsx:591 renders `<Background color="#1f2730" gap={18} size={1} />` on top — two dot layers, two unrelated literals, both must flip for light mode and the TSX one cannot be reached by a CSS var unless the component reads the token (same class of problem as the connectionLineStyle stroke at CanvasWorkspace.tsx:50). Ruling: --surface-canvas (#080b10), --canvas-dot (the rgba slate, whose base ~#93a1b0 incidentally matches the muted-text band — do NOT derive one from the other), --canvas-dot-rf (#1f2730, consumed in TSX via getComputedStyle or a CSS-var-reading prop). Keep #1f2731 (577, popover header divider) in --border-subtle; the 1-digit proximity to #1f2730 is coincidence across a role boundary, not a merge.

**Evidence:** src/styles.css:1112-1121 `.canvas-shell { background: radial-gradient(... rgba(147,161,176,0.13) ...), #080b10 }`; src/components/CanvasWorkspace.tsx:591 `<Background color="#1f2730" gap={18} size={1} />`; styles.css:577 `border-bottom:1px solid #1f2731`

### TM19 [low] Housekeeping merges and traps: case-twin hexes to normalize, #59616a mis-homed under accent-brand, and the adjacent success/error anagram #2f5a3a vs #5a2f33

(1) Case twins to normalize during tokenization: #1C1E20 (126, 328, 418, 1150) vs #1c1e20 (1184, 1189, 2523, 2771); #292B2D (152, 389) vs #292b2d (2848, 2877); #494B4D (451, 1169) vs #494b4d (2927) — same values, casing noise only, but remember 1184/1189 is the text-inverse role, not the surface. (2) #59616a (180) is the idle logo hexagon STROKE — a neutral gray css-1 filed under accent-brand; name it --logo-idle (it retunes against the topbar surface, not against lime). (3) The toast success border #2f5a3a (3251) and error border #5a2f33 (3255) are digit-rotation anagrams FOUR LINES APART — highest typo risk in the whole conversion; tokenize in one commit with a grep check that neither literal survives. Also keep toast green (#2f5a3a/#6fcf86, 3251/3263) as dedicated --accent-success-* per css-4 — it must not alias brand lime.

**Evidence:** src/styles.css:180 `stroke:#59616a`; :3250-3256 `.toast--success { border-color:#2f5a3a } .toast--error { border-color:#5a2f33 }`; case twins at :126/:328/:418/:1150 vs :1184/:1189/:2523/:2771, :152/:389 vs :2848/:2877, :451/:1169 vs :2927

## Light-palette risk assessor（light 表设计风险）（18 条）

### LR1 [high] Brand lime #c7ff00 is 1.18:1 on white — one token cannot serve its 39 uses; split into fill/fg/focus/edge roles

Computed WCAG: #c7ff00 vs #ffffff = 1.18:1, vs near-white #f6f8fa = 1.11:1 (vs dark #101418 = 15.6:1, #1c1e20 = 14.1:1, #080b10 = 16.6:1). Lime is effectively white on a light page. Usage families and verdicts: (a) SOLID FILL carrying hue-tinted dark text — status-valid pill (#151900 on lime = 15.1:1) and edge-remove button — the internal pair survives in light unchanged, only the boundary vs a white page (1.18:1) needs a border or slight fill darkening; a darkened-lime fill also keeps dark text legible (#151900 on #7ca300 = 6.1:1). (b) TEXT/ICON/BORDER on dark surfaces, ~20 sites: diagnostics popover valid title (591), official-check valid button (779-780), palette mode/nav active (927-929, 956-958), palette-add 'small' + is-added (1031, 1037-1044, 1052), port output icon (1394) and compatible/pending state (1405-1406), chip-picker icon/hover (1707, 1713-1714), node status icon (1729), settings summary value (2175), panel-tabs .is-active (2336-2339), JSON viewer title icon (2536), mobile valid icon (2881), mobile row icon (3090), toast action text (3283). There is no lime-on-light today, but in every one of these the underlying surface token flips light, so a naive table produces lime text on light — unreadable. A darkened lime only reaches 4.5:1 around #5d7c00 (4.83:1; #7ca300 = 2.96:1, #6b8f00 = 3.78:1), at which point it reads olive: decide per family whether light keeps a darkened-olive brand-fg (text needs ~#5d7c00, icons/borders can stop at ~#6b8f00 for 3:1) or the role remaps (e.g. active-tab/active-pill could flip to lime-tinted FILL + dark text instead of lime text). Note the existing dim-lime #7ca300 'valid' border (2631) itself fails 3:1 on white (2.96) and must darken too. (c) FOCUS RING and (d) EDGE strokes are separate high findings. (e) LOGO: lime strokes/dots are drawn over a hardcoded #0d1116 hexagon plate (SbcvLogo.tsx:22), idle stroke #59616a is 6.3:1 on white — keeping the dark plate as a theme-exempt brand mark is viable; theming the plate forces a lime redesign. Implication for the table: --accent-brand must split into at least brand-fill (can stay #c7ff00 + border), brand-fg (darkened, pairs with flipped surfaces), brand-focus, and edge-default.

**Evidence:** src/styles.css:470-472 (.status-pill--valid color:#151900/background:#c7ff00), 1862-1863 (edge-remove), 591, 779-780, 927-929, 956-958, 1031, 1037-1044, 1394, 1405-1406, 1707, 1713-1714, 1729, 2175, 2336-2339 (.panel-tabs .is-active background:#101418; color:#c7ff00), 2536, 2631 (.diagnostic--valid border #7ca300), 2881, 3090, 3283; src/components/SbcvLogo.tsx:22 (fill="#0d1116"); styles.css:176-193, 180 (#59616a). Contrast math: node WCAG computation in this audit (lime/white 1.18, lime/#f6f8fa 1.11, #5d7c00 4.83, #6b8f00 3.78, #7ca300 2.96).

### LR2 [high] Lime focus rings and lime alpha glows fail non-text contrast on light — keyboard focus becomes invisible

All focus-visible affordances are lime: 2px solid #c7ff00 outlines on .brand and React Flow control buttons, lime border+color on port action and chip-picker focus, and rgba(199,255,0,0.34) ring on edge-remove focus/hover. At 1.18:1 against white (and the rings at 34% alpha compositing to a near-white tint) the focus indicator disappears entirely in light mode — a WCAG 1.4.11 failure and a real keyboard-usability break since the dark theme relies on outline:none for mouse focus (1193-1195). The light table needs a dedicated --focus-ring token at >=3:1 vs light surfaces (darkened olive or remap to the selection blue family, darkened); the alpha-glow idiom (0.34/0.12/0.09 lime washes at 522, 1407, 1039, 2817, plus the 0.18-opacity lime handle flash at 1573-1576) must be recomputed, not alpha-copied — lime-at-low-alpha over white is white. Glow-as-elevation generally reads weaker on light; consider solid 2px rings instead of soft glows.

**Evidence:** src/styles.css:155-158 (.brand:focus-visible outline 2px solid #c7ff00), 1197-1200 (controls-button:focus-visible), 1192-1195 (mouse focus suppressed, keyboard ring is the only indicator), 1502-1506 (port action focus lime), 1710-1715 (chip-picker focus), 1875-1882 (edge-remove focus ring rgba(199,255,0,0.34)), 522, 1039, 1407, 2817 (alpha-lime washes), 1573-1577 (.sbc-handle.valid lime at opacity 0.18).

### LR3 [high] Edge system: default lime graph and #e4e9ee neutral drag wire vanish on a light canvas; blue/red edge states are borderline

The entire wired graph is lime: .react-flow__edge-path stroke #c7ff00 at 2.2px (1822) and the valid in-flight connection (1587) — 1.18:1 on white, invisible. The neutral drag wire and chip-picker connector are #e4e9ee (1.22:1 on white) — the wire the user is actively dragging disappears in light mode; this one also lives as an inline style in TSX (CanvasWorkspace.tsx:49-54) which the CSS variable table cannot reach, and the invalid-state override already needs !important to beat that inline style (1593-1596 comment). Light values that just darken lime to ~#5d7c00 work numerically (4.8:1) but recolor the product's signature 'green graph'; this is the single biggest brand decision in the plan. Remaining edge states on white: highlighted blue #2d99ff = 2.95:1 (just under the 3:1 UI floor, and it is a meaning-bearing 3px stroke — darken to ~#1c7ed6 = 4.2:1), dangling red #ff5470 = 3.11:1 and invalid #ff5d5d = 3.01:1 (technically pass but are thin dashed strokes over a dotted grid — recommend darkening toward #e34b4b/#c63838, 3.9/5.2:1). Marching-ants animation (RF .animated dash, suppressed for highlighted edges at 1834-1840) inherits whatever stroke is chosen; dashed strokes lose ~half their ink so they need more contrast headroom than solid ones. Tokens should alias per-state: edge-default, edge-valid, edge-invalid, edge-neutral-drag, edge-highlighted, edge-dangling.

**Evidence:** src/styles.css:1821-1824 (default edge #c7ff00), 1586-1588, 1579-1584 and src/components/CanvasWorkspace.tsx:49-54 (neutral #e4e9ee, inline style), 1593-1597 (!important vs inline style), 1617-1624 (chip-picker-link #e4e9ee), 1829-1832 (#2d99ff highlighted), 1834-1840 (marching ants suppression comment), 1844-1847 (#ff5470 dashed dangling). Contrast: e4e9ee/white 1.22, c7ff00/white 1.18, 2d99ff/white 2.95, ff5470/white 3.11, ff5d5d/white 3.01.

### LR4 [high] Warn amber #f2bc4b is 1.74:1 on white — the worst status hue; every amber text/icon/border fails even the 3:1 UI floor

Amber is used as foreground text/icons/borders throughout: popover warning title (595), warning counts (702), official-check warning (784-785), deprecated node badge text (1261), node warning border (1312), node status warning icon (1737), mobile warning icon (2885), popover top border (565), and as the warning pill fill (477, internal pair #1f1600-on-amber = 10.3:1, fine). On white, #f2bc4b = 1.74:1 — below even large-text thresholds; naive inversion produces unreadable warnings, the most safety-relevant text in a validator UI. Light table needs a dark amber/bronze: text requires ~#9a6b00 (4.69:1); the existing dim warn border #a97927 (2627) already measures 3.85:1 and can anchor the UI-level variant. The amber FILL pill can stay saturated with dark text, mirroring the lime pill strategy. Keep the deprecated badge trio (#3a2a0a bg + #f2bc4b text, 1259-1262) paired: light version is a pale amber wash + dark bronze text, not a lightness flip.

**Evidence:** src/styles.css:475-478 (#1f1600 on #f2bc4b pill), 565, 595, 702, 784-785, 1259-1262 (badge trio), 1311-1313, 1736-1738, 2626-2628 (#a97927 = 3.85:1 on white), 2884-2886. Contrast: f2bc4b/white 1.74, 9a6b00/white 4.69, a97927/white 3.85, 1f1600-on-f2bc4b 10.29.

### LR5 [high] Status-hue contrast table: every dark-mode-lightened variant (reds, blues, violet, slate, green, oranges) must DARKEN in light — none survive as-is

Measured on #ffffff (4.5:1 = text, 3:1 = UI): danger base #e34b4b = 3.91 (UI pass, TEXT FAIL — darken text uses to ~#c63838 = 5.22); lightened reds all fail even UI: #ff7b7b 2.51 (node delete icon 1779), #ff9a9a 2.03 (deprecated banner text 2694), #e79a9a 2.21 (error hint 1977), #ff7777 2.57 (mobile error icon 2889), #ff8c69 2.28 (palette Legacy text 1075, toast error icon 3267), #ff5470 3.11 / #ff5d5d 3.01 borderline; the dark-red border #a9444e already passes (5.79, lines 1797/2623). Info blue #2d99ff 2.95 (selection ring/border 1298-1303, corner fill 1322) and its lightened kin #6fb8ff 2.11 (1083), #65b0ff 2.28 (input port icon 1390), #59b4ff 2.23, #9fc6ff 1.75 (channel banner text 2700) all fail; text-grade blue needs ~#1565c0 (5.75). Gated violet #b48ef2 = 2.60 FAIL (1274); platform slate #6fb6e6 = 2.21 FAIL (1267); gated palette tag #8794a0 = 3.10 (passes UI, FAILS as the 'small' text it actually is, 1066). Toast success green #6fcf86 = 1.92 FAIL (3263; needs ~#2e7d32 = 5.13). Checking-status icon #aeb9c6 = 1.99 FAIL (2893). Inspector banner oranges #ffb274 = 1.77, #ffa264 = 1.98 FAIL (2682, 2688). White-on-#e34b4b error pill text = 3.91, below 4.5 for its 12px bold uppercase label (486-487) — light is the moment to retune (darken fill: white on #c63838 = 5.22). Pattern: dark mode LIGHTENED accents for visibility on dark; light mode must DARKEN the same roles — a sign-flipped transform, so the light table cannot be generated by any uniform lightness inversion; each accent needs hand-set fg values.

**Evidence:** src/styles.css:485-488, 698, 790-791, 1066, 1075, 1083, 1266-1267, 1273-1274, 1298-1303, 1322, 1390, 1732-1734, 1779, 1977, 2682, 2688, 2694, 2700, 2889, 2893, 3263, 3267; computed WCAG table in this audit (all numbers above).

### LR6 [high] Four theme-critical colors are hardcoded in TSX and bypass any CSS variable table

Tokenizing styles.css alone ships a broken light mode because four load-bearing values live in components: (1) CanvasWorkspace.tsx:50 — connection-line stroke #e4e9ee as an inline RF style (invisible on light; also the reason CSS needs !important at styles.css:1595-1596); (2) CanvasWorkspace.tsx:591 — <Background color="#1f2730"> dot grid: un-themed it renders 14.2:1 near-black dots over a light canvas (a harsh perforation field; on dark today it is a subtle 1.23:1); (3) SbcvLogo.tsx:22 — hexagon fill #0d1116 (decide: theme-exempt dark brand plate vs themed); (4) ConfigJsonViewerDialog.tsx:72 — CodeMirror theme="dark" (separate finding). These need either CSS-var plumbing (RF accepts var() strings for Background color, or style the SVG pattern via CSS) or theme-aware props wired to the theme state. The plan should include a 'no raw hex in TSX' grep gate alongside the CSS tokenization so new ones cannot land.

**Evidence:** src/components/CanvasWorkspace.tsx:49-54 (connectionLineStyle stroke "#e4e9ee"), 591 (<Background color="#1f2730" gap={18} size={1} />); src/components/SbcvLogo.tsx:22 (fill="#0d1116"); src/components/ConfigJsonViewerDialog.tsx:72 (theme="dark"); src/styles.css:1590-1597 (!important to override the inline stroke). Contrast: 1f2730 vs #f6f8fa = 14.18.

### LR7 [high] Inverse active-control button is absolute white-on-dark-pill — naive tokenization leaves a white button on a white pill

The canvas controls bar paints the engaged tool as a light chip on the dark pill: [data-active="true"] background #f3f4f6 / hover #ffffff with text #1c1e20, sitting on the #1C1E20 controls pill (1150) whose surface token will flip light. If these four literals are mapped to ordinary surface/text tokens (or left absolute), light mode renders white-on-white with dark text floating — the active tool state disappears. They must become a dedicated surface-inverse/text-inverse pair defined relative to the theme (dark chip + light text in light mode), or the active state must be redesigned (e.g. brand-tinted fill). Same trap class as the white-alpha ghost controls: any 'inverted island' inside a themed container needs inverse-relative tokens.

**Evidence:** src/styles.css:1182-1190 (.react-flow__controls-button[data-active="true"] background:#f3f4f6; color:#1c1e20; hover #ffffff), 1143-1152 (.react-flow__controls pill background:#1C1E20). #1c1e20 on #f3f4f6 = 15.2:1 today; both literals flip meaning in light.

### LR8 [high] White-alpha ghost chrome (reveal button, hover washes, inset edge-light) is invisible on light surfaces; its idle text is the undefined-var fallback at 1.9:1

Three white-alpha idioms only work over dark: (1) .field__reveal-button — border rgba(255,255,255,0.12), bg rgba(255,255,255,0.04), hover rgba(255,255,255,0.08), hover text #ffffff: over light input rows every layer composites to ~invisible, and its idle text is var(--color-text-muted, #b5bdc8) — the variable is never defined, and the #b5bdc8 fallback measures 1.90:1 on white, so the Show/Hide sensitive-field control would be unreadable AND unfindable; (2) popover close hover wash rgba(255,255,255,0.05) plus icon brighten #6f7b86 -> #d7dee7 (lighter-on-hover, wrong direction for light); (3) the mobile validation group's inset 0 0 0 1px rgba(255,255,255,0.02) 'edge light' — a dark-only elevation trick that on light is either invisible or a wrong glint; light wants a black-alpha inset or none. Fix pattern: define an --ink channel (white in dark, black in light) and express all ghost washes as ink-alpha tokens; define --color-text-muted for real while doing it.

**Evidence:** src/styles.css:2713-2726 (reveal button, including 2719 color: var(--color-text-muted, #b5bdc8)), 628-631 (close hover rgba(255,255,255,0.05), #6f7b86 -> #d7dee7 at 615/629), 2812-2813 (box-shadow: inset 0 0 0 1px rgba(255,255,255,0.02)). Contrast: b5bdc8/white 1.90.

### LR9 [high] CodeMirror is theme-coupled twice: hardcoded theme="dark" prop and a structural CSS selector keyed to the .cm-theme-dark class name

ConfigJsonViewerDialog.tsx:72 pins theme="dark" (the only CM instance in the app). styles.css keys layout on the theme-derived class: `.json-viewer-editor > .cm-theme-dark { height: 100%; min-height: 0 }` (2578-2581) — flipping the prop to "light" renames the wrapper to .cm-theme-light, the rule stops matching, and the editor loses its height: a layout break, not just colors. The CSS also forces background #0b0f14 on both the wrapper (2574) and .cm-editor (2586), which will fight any light CM theme's own background. Decision for light: (a) keep a dark code island — then the dialog must visually absorb a large dark block on a light chrome (#151a20 dialog at 2511 flips light) and the editor container needs scoped color-scheme:dark so its UA scrollbar (overflow:auto at 2590-2592) stays dark; or (b) go light — then choose a light JSON syntax palette (current colors come from the package's dark theme, not our CSS, so there is nothing to tokenize today), retheme gutters/selection/fold markers, point the forced backgrounds at the surface-code token, and rewrite 2578 to a theme-agnostic selector (e.g. .json-viewer-editor > [class*="cm-theme"] or .cm-editor). Either way theme must become a prop wired to theme state.

**Evidence:** src/components/ConfigJsonViewerDialog.tsx:69-85 (theme="dark", only CodeMirror usage found by repo grep); src/styles.css:2572-2593 (forced #0b0f14 at 2574/2586; name-coupled height rule at 2578-2581; .cm-scroller overflow at 2590-2592).

### LR10 [medium] Tinted-dark accent surfaces (badges, node delete, banners, toasts, lazy-error) must be re-derived as bg/border/fg trios, not lightness-flipped

Roughly a dozen components pair a hue-tinted near-black fill with a lightened accent text: titlebar badges (deprecated #3a2a0a+#f2bc4b, platform #102a3a+#6fb6e6, version #271a3a+#b48ef2), node delete (#2a1416 bg / #5a2a30 border / #ff7b7b text, hover #712d34/#a9444e/#fff), palette Legacy/open borders (#4a2727, #1f3a52), lazy-layer error (#1c1416 bg / #5a2b2b border), toast success/error borders (#2f5a3a, #5a2f33), and the four inspector banners which are already alpha washes (e.g. rgba(168,91,31,0.12) bg + rgba(207,119,47,0.4) border + #ffb274 text). Inverting lightness yields garish pastel-neon; the correct light recipe is: pale tint wash for bg (the existing rgba banner washes actually composite correctly over white and can keep their alphas), a mid-tone border, and a DARKENED accent fg (all current fg values fail on light — see status-hue finding). These must be tokenized as named trios per variant (badge-deprecated-bg/border/fg, banner-platform-..., toast-success-...) so light values can be set independently; a shared 'danger-surface' token cannot serve both the node-delete resting state and its hover. Also preserve the deliberate semantic splits documented in comments: gated = muted+cool vs Legacy = warm (1063-1064), platform = neutral slate not warning (1264), version = violet distinct from amber/blue (1270-1271), and note 'deprecated' is AMBER on node badges but RED in the inspector banner (2691-2694) — same word, two hue systems the light table must not accidentally unify.

**Evidence:** src/styles.css:1259-1274 (three badge trios + design comments), 1766-1798 (node delete rest/hover trios), 1074-1087 (Legacy/open), 2103-2116 (lazy-layer error #5a2b2b/#1c1416), 2679-2700 (four banner trios incl. rgba washes), 3250-3267 (toast borders/icons), 1063-1064 and 1264-1271 (intent comments).

### LR11 [medium] Status pill fills: boundary vs white page, and brightness(1.1) hover pushes the wrong direction on light

The topbar validation pill is a saturated fill on the pill bar: lime/amber/red fills with hue-tinted dark text (valid/warning) or white text (error). In light mode: (1) the lime pill body is 1.18:1 against a white page and the topbar pill chrome flips light, so the pill silhouette needs a border or a slightly darkened fill (internal text pairs survive: #151900-on-lime 15.1:1, #1f1600-on-amber 10.3:1; white-on-#e34b4b is 3.91 and should be fixed by darkening the red fill, see status finding); (2) hover is `filter: brightness(1.1)` (and 1.14 on edge-remove) plus a lime alpha ring — brightening a saturated fill on a white page washes it toward the page color, the exact opposite of the pressed/darker cue light themes need; the hover token should flip to brightness(<1) or a darker fill + shadow. The 'checking' pill (#d7dee7 on #20262d) is a light-on-dark pair that must also flip. These pills are the app's primary status signal; treat fill+text+ring as one retuned unit per state.

**Evidence:** src/styles.css:466-488 (four pill fills incl. comment about bright fill), 498-535 (hover re-assert + filter: brightness(1.1) + rgba rings at 519-535), 1875-1882 (edge-remove brightness(1.14)), 480-483 (checking pair).

### LR12 [medium] Shadow scale is tuned for dark (alpha 0.28-0.6) — on white it reads as smudges; tokenize whole box-shadow strings per elevation

Heaviest shadows by line: 2961 (bottom sheet, 0 -24px 60px rgba(0,0,0,0.6)), 2512 (JSON dialog, 0 32px 90px @0.58), 554 (diagnostics popover, 0 24px 60px @0.55), 329 (@0.52), 853 (0 28px 90px @0.5), 2116 and 2772 (@0.5), 2305 (@0.48), 127/419/1151/1639/1304 (@0.46), then a 0.34-0.42 band on nodes/ports/toasts (1287, 1386, 1407, 1864, 3244). Light-mode strategy: keep black-based shadows but cut alpha to roughly a third (~0.10-0.18) and tighten spread, leaning on borders for edge definition (the dark theme already pairs every shadow with a 1px border, which helps); recommend collapsing into 3-4 elevation tokens that hold the ENTIRE shadow string (--shadow-overlay, --shadow-raised, --shadow-node, --shadow-glow-*) so light can also adjust geometry, not just color. Scrims can stay black-family in light (standard practice): 2499 rgba(3,6,10,0.68)+blur(4px) and 2950 rgba(0,0,0,0.4)+blur(3px) — possibly trim to ~0.3-0.45. Separate trap: backdrop-filter blur(14px/16px) currently sits under OPAQUE fills (#1C1E20 pills at 126-128, 418-420, 1150-1152; #1c1e20-ish panel 2772-2773) so it is a no-op; if the light theme makes those pills translucent white, the blur silently activates over the canvas — decide opaque vs frosted explicitly and budget the GPU cost.

**Evidence:** src/styles.css:2961, 2512, 554, 329, 853, 2116, 2772, 2305, 127, 419, 1151, 1639, 1304, 1287, 1386, 1864, 3244 (shadows); 2496-2501, 2947-2953 (scrims); 126-128, 418-420, 1150-1152, 2773 (blur under opaque fills).

### LR13 [medium] Canvas: two stacked 18px dot grids, card-lighter-than-canvas polarity, and a minimap that rides React Flow's light-biased defaults

The canvas backdrop is BOTH a CSS radial-gradient dot grid (rgba(147,161,176,0.13) 1px dots over #080b10, background-size 18px) and an RF <Background> dot layer (#1f2730, gap 18) — two systems that must take the same token or be consolidated during tokenization. Light canvas needs: dots as black-alpha (~rgba(20,30,40,0.12-0.18)) so they stay subtle (the hardcoded #1f2730 would be 14.2:1 dots — see TSX finding); and a deliberate surface split where the canvas sits slightly BELOW the cards (e.g. light-gray canvas, white cards) because the dark theme's separation comes from cards being LIGHTER than the canvas (#1a1e21 card vs #080b10 canvas) plus heavy shadows — both cues weaken on light, so card borders (#30363d today, 1284) must carry more of the separation. Inset-on-canvas elements need the same review: ports are #101418 chips sitting directly on the canvas (1384) and the minimap surface is #11161c (1139-1141). The minimap is only surface-themed: no maskColor/nodeColor props (CanvasWorkspace.tsx:611) and no other .sbc-minimap rules, so mask/node fills are RF defaults (light-gray mask rgb(240,240,240,0.6), light node fills) which currently read as a light viewport-window on dark; in light mode those defaults blend into a light minimap and the viewport mask becomes nearly invisible — set RF's --xy-minimap-* variables (or props) per theme, for both themes, while pinning the current dark look.

**Evidence:** src/styles.css:1112-1122 (.canvas-shell radial-gradient dots + #080b10, background-size 18px 18px), src/components/CanvasWorkspace.tsx:591 (Background #1f2730 gap 18), styles.css:1282-1287 (card #1a1e21 + border + shadow), 1376-1386 (port #101418 on canvas), 1139-1141 (only minimap rule), CanvasWorkspace.tsx:611 (MiniMap with no color props).

### LR14 [medium] Selection-blue system is marginal on light and its corner handles rely on a near-white outline that disappears

Node selection paints border + 2px ring in #2d99ff (2.95:1 on white — just under the 3:1 UI minimum for a state indicator) plus first-degree edge highlighting at the same hue (1830). The selection corner handles are #2d99ff squares outlined in #e7f4ff (1.12:1 on white) with a rgba(45,153,255,0.3) halo — on dark, the near-white outline separates the handle from the card; on light it evaporates and the halo weakens, leaving low-contrast blue squares floating on white cards. Light table: darken selection blue toward ~#1c7ed6 (4.2:1) for ring/border/edges, flip the handle outline to a dark ink or theme-surface color, and recompute the halo alpha against white. Keep ring и border the same token so the hue shift stays coherent with highlighted edges.

**Evidence:** src/styles.css:1296-1305 (.sbc-node.is-selected border #2d99ff + 0 0 0 2px #2d99ff ring), 1315-1324 (.sbc-node__corner border 2px #e7f4ff, background #2d99ff, halo rgba(45,153,255,0.3)), 1829-1832 (highlighted edges). Contrast: 2d99ff/white 2.95, e7f4ff/white 1.12, 1c7ed6/white 4.20.

### LR15 [medium] Muted-gray text ramp sits at ~2.0-3.1:1 against white — the entire de-emphasized text tier must be re-anchored for light, not inverted

The dark theme's muted text cluster (#8a96a3 / #8f9aa5 / #8d98a3 / #9aa5b0 / #8794a0 etc.) measures ~2.8-3.1:1 on white; the dimmer tier (#6f7b86, #65707b, #76818c, #707b86) and the bright fallback #b5bdc8 (1.90) land between 1.9 and ~4. None pass 4.5:1 for the captions/hints/section-labels they render. Because dark muted = mid-gray and light muted also = mid-gray (~#5b6673 territory for 4.5+), this tier is the one place a naive 'reuse the same grays' shortcut looks plausible and fails: every merged --text-muted/--text-secondary token needs a hand-picked darker light value, and the planned merge of the 15-value ramp should decide which uses are genuinely decorative (placeholder #717c88, disabled #707b86/#73808b at 1070-1071/2181-2182 may stay sub-4.5 by design) versus informative (palette group headers 870-871, doc-link 1096, popover summary 606, node subtitle 1756) which must clear 4.5. Also flip the global ink: text-primary #eef2f4 family becomes near-black, and disabled styling via opacity:0.45 (50-53) reads differently over light fills — re-check it.

**Evidence:** src/styles.css:606 (#8a96a3), 870-871 (#6f7b86 header), 922, 952, 972, 1066, 1071, 1096, 1756 (#8c949d), 2181-2182, 2719 (#b5bdc8 fallback), 31-53 (global button text + opacity-disabled). Contrast: 8a96a3/white 3.01, 8794a0/white 3.10, b5bdc8/white 1.90.

### LR16 [medium] color-scheme flip side-effects: every scrollbar in the app is UA-rendered dark today; dark islands will need scoped color-scheme

color-scheme: dark exists once (styles.css:2) and there is zero custom scrollbar CSS in the repo, so ALL scroll surfaces ride the UA dark scrollbar: diagnostics list (637 overflow-y), chip-picker list (1677), CodeMirror scroller (2590-2592), mobile topbar overflow-x (2666), palette/inspector panels. The theme mechanism must set color-scheme per theme on :root (light gets light scrollbars automatically — most visible on Windows/Linux non-overlay scrollbars where a missed flip leaves chunky dark tracks on white panels). Inverse risk: any deliberately-dark island kept in light mode (CodeMirror editor, potentially the logo plate) needs its own scoped color-scheme: dark or it gets a light scrollbar inside a dark box. Also affected by the flip: native select dropdown menus, checkbox/calendar chrome, and input autofill tinting for the form controls styled at 55-69 (their custom dark fills currently match the UA dark menus; in light the custom fills must flip with them). index.html has no <meta name="theme-color"> and no color-scheme meta — add a theme-synced theme-color if mobile browser chrome should follow (low stakes), and note the three-state system mode must mirror into color-scheme, not just data-theme, to keep scrollbars synced.

**Evidence:** src/styles.css:2 (sole color-scheme declaration; repo grep for 'scrollbar' returns nothing else), 633-638, 1672-1680, 2590-2593, 2665-2667 (scroll surfaces), 55-69 (native control styling); grep of index.html found no theme-color/color-scheme meta.

### LR17 [medium] Hover/active polarity inventory: dark theme signals interactivity by LIGHTENING — every hover token needs an explicitly darker light value

Families whose polarity must flip (base -> hover, all brighter today): global buttons #101418 -> #171d24 with border #29313a -> #5f6d7a (31-48); brand pill transparent -> #292B2D (151-153) and mobile status hover #292b2d (2876-2878); RF controls #333537 -> #494B4D + text -> #fff (1161-1171); doc-link #101418 -> #171d24 + text #8f9aa5 -> #eef2f4 (1106-1110); JSON viewer actions #252b32 -> #303842 (2556-2565); mobile rows -> #20262d (3082-3084); ports #101418 -> #151b22 + border lighten (1397-1401); official-check -> #1a222b (769-771); node icon buttons -> text #fff (1808-1812). Plus the non-background lighteners: text-brighten-on-hover (#6f7b86 -> #d7dee7 at 615/629; #b5bdc8 -> #ffffff at 2719/2725), border-brighten (#29313a -> #5f6d7a), and filter: brightness(1.1)/(1.14) on saturated fills (521-533, 1880). If the variable table maps each base surface to a light value but reuses the dark deltas, hover will LIGHTEN toward white and read as disabled/washed-out; light values must make hover darker than base (or border-driven). Practical consequence for the token design: hover states cannot be derived (no calc/lightness offset shared across themes) — every surface-hover and text-hover pair must be an independent token with hand-set values per theme. Low-risk same-polarity exception: the skeleton shimmer highlight (#1a1f24 -> #20262c -> #1a1f24, 2093) stays lighter-than-base in both themes but still needs a light pair.

**Evidence:** src/styles.css:31-48, 147-153 (comment documents the hover-lighten intent), 615/628-631, 769-771, 1106-1110, 1161-1171, 1397-1401, 1808-1819, 2556-2565, 2719-2726, 2876-2878, 3082-3084, 519-535 and 1875-1882 (brightness filters), 2088-2095 (shimmer).

### LR18 [low] Low-stakes cleanups the light table should fold in: case-duplicate #1C1E20, checking-pill pair, grabber, lime-tinted text partners

(1) #1C1E20 (uppercase, topbar/controls/JSON header at 126, 328, 418, 1150, 2523) duplicates lowercase #1c1e20 used elsewhere as text-inverse (1184) and sheet surface — alias both spellings to one token now or the light override will miss half of them. (2) status-pill--checking (#d7dee7 on #20262d, 480-483) and the bottom-sheet grabber #4d5663 (2984-2989) are small light-on-dark pairs that need flipped values but no design decisions. (3) Lime-adjacent tinted texts must move WITH their partner token, not to generic text tokens: #151900/#1f1600 pill inks (471, 476), #f6ffe8 on the lime-washed is-added row (1040), and the rgba(199,255,0,0.09) wash behind it (1039). (4) The dark-red on-accent text #fff usages on danger fills (487, 1798, 1816) should become text-on-danger so the light table can retune them with the fill. None of these look broken if missed individually, but each is a pixel-drift risk for the 'dark stays identical' guarantee since they are easy to mis-bucket during the literal-to-token sweep.

**Evidence:** src/styles.css:126, 328, 418, 1150, 2523 (uppercase #1C1E20) vs 1184, 2957-area lowercase usage; 480-483; 2984-2989; 471, 476, 1039-1040; 487, 1795-1798, 1814-1818.
