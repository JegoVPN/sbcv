<!-- Status: audit (2026-05-27). Cross-check of `docs/goals/editable-node-ui-deep-pass.md` claimed implementation against actual code in `src/`. 66 nodes reviewed by 66 parallel agents in 7 batches; only code (not doc claims) was trusted. -->

# Editable Node UI Deep Pass — Code Audit 2026-05-27

This audit cross-checks the [editable-node-ui-deep-pass goal](goals/editable-node-ui-deep-pass.md) against actual source code. 66 nodes were reviewed in 7 batches of parallel agents; each agent read the node's two review docs (`docs/ui-reviews/<node>.md` + `docs/claude/<node>.md`) only to extract the claimed P0/P1 findings, then grepped the codebase to verify whether each claim is actually implemented.

**Result: ~120 deltas across 60+ of 66 nodes.** Doc status often says `ui-verified` or `implemented`, but the underlying code is partial or missing. Five nodes are clean: `outbound-vless`, `outbound-tor`, `dns-server-quic`, `dns-server-udp`, `service-ocm`.

This audit is the source of truth for the remaining implementation work. Do not trust the per-node review docs' status comments until the deltas below are closed.

## Cross-cutting Defects (Fix First)

These appear in many nodes simultaneously. One PR per item can close 10–30 nodes' P0s at once.

### CC-1 `address` / `auto_route` rendered for every inbound (should be tun-only) — ✅ CLOSED 2026-05-27

- ~~`src/components/Inspector.tsx:2268-2282` unconditionally renders `Address` + `auto_route` for **all** inbound types, with no `entityType === "tun"` guard.~~ Now gated by `entityType === "tun"` wrapper.
- **Affected (now fixed):** mixed / socks / http / shadowsocks / vmess / trojan / naive / hysteria / hysteria2 / tuic / vless / shadowtls / anytls / redirect / tproxy.
- **Regression test:** `tests/app.test.tsx` — "hides address/auto_route fields for non-tun inbounds" + "renders address/auto_route fields for inbound:tun".

### CC-2 Required protocol fields fall through to `AdvancedScalarFields`

- `password` / `uuid` / `username` / `network` enum are declared in `outboundHandledFields` or `inboundHandledFields`, but no dedicated first-class control exists inside the matching `entityType ===` branch — so the field renders as a generic raw input under "Advanced".
- **Confirmed missing first-class control:**
  - inbound `shadowsocks` → `password`, `users[]`, `destinations[]`, `network`, `managed`
  - inbound `http` → `set_system_proxy` platform-restriction banner
  - inbound `tuic` → `users` entirely
  - inbound `shadowtls` → `version` select
  - outbound `shadowsocks` / `trojan` / `tuic` → `password`
  - outbound `tuic` → `uuid`, `network`, `heartbeat`, `zero_rtt_handshake`
  - outbound `socks` → `network` enum, `username`/`password` first-class
  - outbound `http` → `headers` map (entirely invisible), `path` not in `outboundHandledFields`

### CC-3 Scaffold hardcodes `network: "tcp"` / `"udp"` instead of omitting — ✅ CLOSED 2026-05-27

- ~~Official semantics: omit `network` to mean "both TCP and UDP". The scaffold lies to the user.~~ All 10 hardcoded `network:` defaults stripped from `createInbound` / `createOutbound`.
- **Affected (now fixed):** inbound — direct, naive, tproxy. outbound — shadowsocks, vmess, vless, trojan, hysteria, hysteria2, tuic.
- **Regression test:** `tests/domain.test.ts` — "omits the network field from scaffolds so sing-box defaults to both TCP and UDP".

### CC-4 Palette kind naming still inconsistent (M1.8 incomplete) — ✅ CLOSED 2026-05-27

- ~~`Palette.tsx:132` uses `kind: "mixed"` instead of `"inbound-mixed"`.~~ Now `inbound-mixed`.
- ~~`Palette.tsx:145` uses `kind: "tun"` instead of `"inbound-tun"`.~~ Now `inbound-tun`.
- ~~`Palette.tsx:92` still ships `dns-http3` instead of `dns-h3`.~~ Now `dns-h3`.
- Lookup keys in `INBOUND_PALETTE_TYPES` (`protocols.ts`) and `OUTBOUND_PALETTE_TYPES` updated to match; affected test files updated.

### CC-5 TLS-required / users-required diagnostics missing for several protocols — ✅ MOSTLY CLOSED 2026-05-27

- **Audit re-verification:** `outbound-missing-tls` already covers all 7 TLS-required outbound types (trojan / naive / hysteria / hysteria2 / tuic / anytls / shadowtls). The audit's claim that hysteria/hysteria2/tuic were uncovered was incorrect — diagnostic exists at `diagnostics.ts:396-411`. Regression test added.
- **users-required gap fixed:** New `inbound-users-required` error covers all 8 authenticating inbound types (vmess / vless / trojan / naive / hysteria / hysteria2 / tuic / anytls) — previously only vmess/vless/tuic had per-user UUID checks via `validateVmessLikeUsers` and even those didn't error on an empty `users[]`.
- **service derp:** `derp-service-needs-tls` warning already exists at `diagnostics.ts:211`. Scaffold still doesn't seed `tls: { enabled: true }` — see service-derp row below for that piece.

### CC-6 Canvas port action-gating not honoured — ✅ CLOSED 2026-05-27

- ~~`graph.ts:313` and the equivalent dns-rule block create outbound/dns-server edges regardless of `action`.~~ Now gated. Route rule outbound edge renders only when `action ∈ {"", "route", "bypass"}`; dns rule server edge renders only when `action ∈ {"", "route", "evaluate"}`. Other actions (reject/sniff/route-options/hijack-dns/resolve/predefined/respond) suppress the dangling reference visually.
- **Regression test:** `tests/domain.test.ts` — "hides route-rule outbound canvas edge when action is not route/bypass" + "hides dns-rule server canvas edge when action is not route/evaluate".

### CC-7 Platform / channel badges declared in code but not surfaced on Palette — ✅ CLOSED 2026-05-27

- ~~`redirect`, `tproxy`, `hysteria-realm` still show `status: "setup"` on the Palette instead of a gated badge with the correct gate reason.~~ Labels now carry the gate inline: `Redirect (Linux only)`, `TProxy (Linux only)`, `Resolved Server (Linux only)`, `Resolved (Linux only)`, `Hysteria Realm (1.14 testing)`, `DERP (with_tailscale)`, `Tailscale Server (with_tailscale)`, `Tailscale (with_tailscale)` endpoint, `Tor (with_tor)`. Status stays `setup` so the items remain addable; the suffix is visible at-a-glance in the search list and screen-reader-friendly.

## Node-Specific P0 Defects

Grouped by family. Each row is one verified delta from running the audit; line numbers are approximate (codebase moves).

### Settings

| Node | Defect |
| --- | --- |
| settings-log | ✅ Closed 2026-05-27. All claims verified; the one remaining P1 idea ("log.level enum value validation") is out of scope for this audit — invalid level values produce a sing-box startup error which already surfaces via the official-check pipeline. |
| settings-ntp | ✅ ~~NTP detour canvas edge never generated~~ — fixed 2026-05-27; `graph.ts` now emits `edge:settings-ntp-detour:<tag>` when `config.ntp.detour` is set, and `SbcNode.tsx` exposes a `dial-detour` output port on `kind === "settings"` + `type === "ntp"` so the connection is visible. Canvas title still says "Ntp" (path-capitalised) — minor cosmetic, not blocking. |
| settings-certificate | ✅ ~~PEM textarea comma-split~~ — fixed. ✅ ~~Missing diagnostics for `store: "chrome"` 1.13+ gate and the 1.12+ `certificate` block channel gate~~ — fixed 2026-05-27, two new stable-channel warnings: `settings-certificate-block-testing-only` (top-level block) and `settings-certificate-store-chrome-testing-only` (chrome store specifically). `certificate_path[]` / `certificate_directory_path[]` still use comma-separated input (acceptable; file paths rarely contain commas). |
| settings-experimental | ✅ Mostly closed 2026-05-27. ~~`experimental.v2ray_api.stats.inbounds` / `outbounds` / `users` not rendered~~ fixed — V2Ray API ModuleCard now renders three CSV inputs after the `Enable stats` toggle (testids `v2ray-stats-inbounds` / `-outbounds` / `-users`), each binding to the structured `experimental.v2ray_api.stats.{inbounds|outbounds|users}` array and clearing the key on empty CSV. ~~No v2ray stats tag-existence diagnostics~~ fixed — two new errors: `v2ray-stats-inbound-missing` and `v2ray-stats-outbound-missing` fire when a stats array references a tag that doesn't exist in `config.inbounds` / `config.outbounds`. (Users array intentionally has no diagnostic — those are vmess/vless usernames inside inbound `users[]`, an opt-in pattern, and the false-positive rate would be high.) Two regression tests in domain.test.ts (positive + negative). The remaining sub-palette stubs (`experimental-cache-file` etc.) stay as no-ops — those palette entries are documentation pointers, not constructible nodes, so they intentionally don't have createFromPalette handlers. `SbcNode.tsx` iconMap is cosmetic and out of scope. |

### Hub

| Node | Defect |
| --- | --- |
| hub-route | ✅ ~~5 platform fields missing~~ — fixed 2026-05-27. ✅ ~~sharedFieldRegistry.ts:200 unconditionally pushes http-client + neighbor groups~~ — fixed 2026-05-27, `sharedGroupsForEntity` now takes a `channel` argument and hides `http-client` + `neighbor` groups on stable channel (covers route hub, route-rule, dns-rule). Inspector passes the current channel. |
| hub-dns | ✅ Closed 2026-05-27. ~~No embedded `fakeip` editor in the dns hub branch~~ fixed — dns hub Inspector now renders a `FakeIP` fieldset (testid `dns-hub-fakeip`) with an `enabled` toggle plus `IPv4 Range` (placeholder `198.18.0.0/15`) and `IPv6 Range` (`fc00::/18`) inputs, all wired to the structured `dns.fakeip` object and cleaning up unset keys on round-trip. ~~`optimistic` / `timeout` (testing 1.14) have no channel gate~~ fixed — added `dns-optimistic-testing-only` and `dns-timeout-testing-only` warnings under the `channel === "stable"` block alongside an inline `channel` PlatformBanner above both new controls. Two regression tests in app.test.tsx + domain.test.ts lock the UI write-through and the channel-gate behavior on stable vs testing. |

### Rules

| Node | Defect |
| --- | --- |
| rule-route-rule | ✅ ~~`action === "route-options"` sub-form not implemented~~ — fixed 2026-05-27; Inspector now renders a `Route options` fieldset (override_address / override_port / network_strategy / fallback_delay / udp_disable_domain_unmapping / tls_fragment) whenever the rule's action is "route" or "route-options". All six fields added to routeRulePrimaryFields so AdvancedScalarFields no longer double-renders them. This also gives 1.11-D users a concrete migration target. Deprecated `geosite` / `source_geoip` / `geoip` still in `routeRuleAdvancedFields`. `inbound` / `rule_set` references still free-text via `RuleListField`. ✅ canvas outbound port action-gating fixed under CC-6. |
| rule-dns-rule | ✅ ~~Phantom `dns-rule-action` Palette kind still present~~ — removed 2026-05-27; `dns/rule_action.md` now mapped to "no direct palette" (action lives inside the dns-rule Inspector). Canvas dns-server port gating fixed under CC-6. Missing 1.13 stable fields `interface_address`, `network_interface_address`, `default_interface_address`. `package_name_regex` (1.14 testing) absent. `evaluate` / `respond` / `route-options` action sub-forms not rendered. |

### Rule-set

| Node | Defect |
| --- | --- |
| rule-set-local | ✅ ~~`SbcNode.tsx:169` returns `download-detour` port for all rule-set kinds~~ — fixed 2026-05-27; SbcNode now returns an empty output port array unless `type === "remote"`, so inline and local rule-set nodes no longer expose a dangling `download-detour` wire. ✅ Palette three entries already landed. |
| rule-set-remote | ✅ Mostly closed 2026-05-27. ~~No `missing-rule-set-format` diagnostic~~ fixed — new `rule-set-format-missing` error fires when a remote rule_set has no `format` AND the URL's path component doesn't end in `.json` or `.srs` (the only two extensions sing-box auto-infers per `rule-set/index.md`). Strips query string + fragment before extension lookup so `?v=2` doesn't fool it. Two regression tests in domain.test.ts lock positive (.txt without format) + negative (.srs / .json / format=binary). ~~Palette label still generic "Rule Set"~~ false positive — Palette.tsx:183 already reads `Remote Rule Set`. The remaining gap (`http_client` inline object form vs string select) stays: when users need the rare inline-object form, the field round-trips via AdvancedNonScalarFields, which is the documented escape hatch for sub-object editing. |
| rule-set-inline | ✅ ~~hardcoded to remote~~ — fixed. ✅ ~~Inline node still shows `download-detour` port~~ — fixed 2026-05-27; SbcNode suppresses the port for inline/local rule-sets. Canvas subtitle still generic. |

### Inbound

| Node | Defect |
| --- | --- |
| inbound-direct | ✅ ~~`network`, `override_address`, `override_port` only as raw inputs in Advanced~~ — fixed 2026-05-27, inbound direct now has a dedicated block with `Network` select (`(both)` / `tcp` / `udp`), `Override Address` text input (placeholder `1.1.1.1`), and `Override Port` numeric input (placeholder `53`). `override_address` and `override_port` added to `inboundHandledFields`. `network` stays out of the shared inboundHandledFields set since other inbound types still rely on AdvancedScalarFields for that key — direct just shadows it locally. Regression test "renders inbound:direct network select + override_address/override_port" locks all three controls and the resulting numeric port (Number, not string). |
| inbound-mixed | ✅ Closed 2026-05-27: ~~Palette `kind: "mixed"`~~ resolved by CC-4 (now `inbound-mixed`). ~~`set_system_proxy` toggle not in inbound block~~ fixed — added a dedicated toggle row for `mixed` / `http` / `socks` inbounds (the three system-proxy capable types per inbound/mixed.md). Label notes the supported platforms (Linux / Android / Windows / macOS). `set_system_proxy` added to `inboundHandledFields`. Regression test "renders inbound:mixed/http/socks set_system_proxy toggle in dedicated row" loops through all three types. ~~`address`/`auto_route` shown despite mixed not supporting them~~ resolved by CC-1 (tun-only gating). |
| inbound-socks | ✅ Closed 2026-05-27. `address`/`auto_route` rendered only for `entityType === "tun"` (Inspector.tsx:2458) — CC-1 verified. `set_system_proxy` now covered by the shared toggle row at the mixed/http/socks block (Atomic 66). |
| inbound-http | ✅ Closed 2026-05-27. `address`/`auto_route` not rendered (CC-1 verified). `set_system_proxy` toggle now present with an inline "Linux / Android / Windows / macOS" platform note (Atomic 66) — covers the platform restriction warning gap. |
| inbound-shadowsocks | ✅ ~~`method` enum not first-class for inbound~~ — fixed 2026-05-27; Inspector renders a full 13-entry method select (2022 + AEAD + legacy stream cipher) inside `entityType === "shadowsocks"` inbound block, and `method` added to `inboundHandledFields` so AdvancedScalarFields doesn't double-render. `users[]` rendered via INBOUND_USER_SCHEMAS. `password`, `destinations[]`, `network`, `managed` still fall through to AdvancedScalarFields. |
| inbound-vmess | ✅ ~~UUID **generate button missing** inside users repeater~~ — fixed 2026-05-27, INBOUND_USER_SCHEMAS-driven users editor now wraps the UUID `SensitiveTextField` in a row that adds a per-user `Generate UUID` button (aria-label `Generate UUID for user N`) calling `crypto.randomUUID()` and patching `users[i].uuid`. Regression locked in app.test.tsx ("renders a per-row Generate UUID button inside vmess/vless/tuic users editor"). `protocols.ts:175` reverse map says `vmess: "vmess-in"` but Palette kind is `"inbound-vmess"` — any code path using the reverse lookup will fail. alterId deprecation warning exists in diagnostics but not inline next to the field. |
| inbound-trojan | ✅ Mostly closed 2026-05-27: ~~users have no protocol-specific UI~~ already covered by INBOUND_USER_SCHEMAS schema-driven editor; ~~fallback has no UI~~ fixed — inbound trojan block now renders a Fallback Server fieldset with `Server` + `Server Port` inputs producing a structured `{server, server_port}` object; key added to `inboundHandledFields`; regression test "renders inbound:trojan fallback Server/Port inputs that build a structured fallback object" locks the shape. ~~TLS-required diagnostic absent~~ false positive — trojan is in `tlsRequiredInboundTypes` at diagnostics.ts:365 (CC-5). ~~Listen 5 fields not surfaced~~ false positive — `listenSharedFields` already covers `listen`/`listen_port`/`tcp_fast_open`/`tcp_multi_path`/`udp_fragment`. ~~TLS card missing `key_path` (server-side)~~ false positive — `Key Path (server)` is rendered at Inspector.tsx:1497. `fallback_for_alpn` remains in Advanced JSON since it's an ALPN-keyed map; rare config. |
| inbound-naive | ✅ ~~`network` and `quic_congestion_control` enum selectors missing~~ — fixed 2026-05-27, inbound naive block now renders a `Network` select (`(both)` / `tcp` / `udp`) and a `QUIC Congestion Control` select with the full 1.13+ 6-value enum (`bbr` / `bbr_standard` / `bbr2` / `bbr2_variant` / `cubic` / `reno`). `quic_congestion_control` added to `inboundHandledFields`. Regression test "renders inbound:naive network + quic_congestion_control as first-class enum selects" locks the control types + option sets + write-through. users non-empty check is now covered by the cross-cutting `inbound-users-required` diagnostic (CC-5). |
| inbound-hysteria | ✅ ~~No v1 deprecation banner in inbound block~~ — fixed 2026-05-27, inbound block now renders the same `PlatformBanner kind="deprecated"` for `entityType === "hysteria"` that the outbound block had. ✅ `inbound-hysteria-v1-deprecated` diagnostic also added. Palette label still "Hysteria" (could add "(v1, legacy)" suffix as polish). `up_mbps`/`down_mbps` have no protocol-specific "Required" annotation. |
| inbound-hysteria2 | ✅ ~~`masquerade` field completely absent + `brutal_debug` falls to AdvancedScalarFields~~ — fixed 2026-05-27, inbound hysteria2 block now renders `Masquerade (URL)` text input (placeholder `http://127.0.0.1:8080 or file:///var/www`, matching the URL-form documented at inbound/hysteria2.md `#### masquerade`) and a `Brutal Debug` toggle row. Both keys added to `inboundHandledFields`. Object-form masquerade (status/headers/content/proxy) remains in Advanced JSON since it's a rare config; URL form covers the dominant case. Regression test "renders inbound:hysteria2 masquerade URL + brutal_debug toggle" locks both controls. TLS-required diagnostic is covered by the cross-cutting `tlsRequiredInboundTypes` (CC-5 fix) which includes hysteria2. obfs/min_packet_size/max_packet_size still only via JsonField. |
| inbound-tuic | ✅ ~~`users` array completely invisible~~ — false positive in audit; tuic IS in `INBOUND_USER_SCHEMAS` (Inspector.tsx:551, fields name + uuid + password), and the schema-driven users editor renders for any inbound type that appears in the registry. Regression test added in app.test.tsx ("renders tuic inbound users[] with structured Name/UUID/Password fields"). `auth_timeout`, `heartbeat`, `zero_rtt_handshake` still missing first-class controls. TLS-required already enforced via the `outbound-missing-tls`-style check on `tlsRequiredInboundTypes` which includes tuic. |
| inbound-vless | ✅ ~~`users[].flow` in repeater is plain text~~ — fixed 2026-05-27, INBOUND_USER_SCHEMAS gained optional `enum` array per field; the users repeater renders enum fields as `<select>` instead of free-text input. `vless.flow` enum = `[(none), xtls-rprx-vision]`. Regression test "renders vless users[].flow as an enum select bound to xtls-rprx-vision" locks the control type + values + write-through. |
| inbound-shadowtls | ✅ ~~No version select~~ — fixed 2026-05-27, inbound shadowtls block now renders a `Version` `<select>` with 3 labeled options (1 no-auth / 2 single-user / 3 multi-user via users[]); `version` added to `inboundHandledFields`. No version-gated visibility for v2/v3 password vs users. `handshake_for_server_name` no repeater/UI. No version-validation diagnostics. |
| inbound-anytls | ✅ ~~`padding_scheme[]` completely invisible~~ — fixed 2026-05-27, inbound anytls block renders a Padding scheme textarea (one rule per line, splits on `\n`); `padding_scheme` added to `inboundHandledFields`. ✅ `address`/`auto_route` fixed under CC-1. ✅ 1.12+ version gate is N/A. |
| inbound-tun | ✅ Mostly closed 2026-05-27. ~~Missing UI for include/exclude UID/interface/package arrays + auto_redirect~~ fixed — tun inbound block now renders six CSV inputs (`Include UID range (Linux, CSV)`, `Exclude UID range (Linux, CSV)`, `Include Interface (CSV)`, `Exclude Interface (CSV)`, `Include Package (Android, CSV)`, `Exclude Package (Android, CSV)`) plus an `Auto redirect (Linux, requires auto_route)` toggle row. Each emits an `undefined`/array round-trip and is added to `inboundHandledFields` so the Advanced section doesn't double-render. Regression test "renders tun include/exclude UID/interface/package CSV inputs + auto_redirect toggle" sets all seven controls and asserts the parsed values land in the store. ✅ ~~`createInbound` scaffold is IPv4-only~~ — fixed earlier, dual-stack `["172.19.0.1/30", "fdfe:dcba:9876::1/126"]`. The bare `include_uid[]` / `exclude_uid[]` / `include_android_user[]` (without `_range`) variants and the 4 auto_redirect companion fields (input_mark/output_mark/etc.) remain in Advanced JSON — promoting every Linux-only knob would dwarf the rest of the Inspector and 95% of users only need the range and interface filters covered here. |
| inbound-redirect | ✅ Closed 2026-05-27. Linux-only `platform` PlatformBanner now rendered above the redirect block ("Linux-only inbound: redirect uses Linux netfilter primitives ..."), covering the spirit of the missing diagnostic without a duplicate severity record. `address`/`auto_route` false positive — already CC-1 gated to tun only. Palette status `"setup"` is intentional (the Linux-only label communicates the restriction; gating prevents creation on non-Linux dev machines which is too aggressive for a config editor). |
| inbound-tproxy | ✅ Closed 2026-05-27. Palette label is `"TProxy (Linux only)"` (Palette.tsx:146) — CC-4/7 already surfaces the platform restriction in the picker. New: Inspector now renders a `platform` PlatformBanner above the tproxy block ("Linux-only inbound: tproxy uses Linux netfilter primitives and will not bind on macOS/Windows/iOS.") and a dedicated `Network` enum select with `(both)` / `tcp` / `udp` options. Regression test "renders Linux-only banner on tproxy + redirect inbounds and a network enum on tproxy" locks both. |

### Outbound

| Node | Defect |
| --- | --- |
| outbound-direct | ✅ ~~`domain_strategy` 1.12+ deprecation diagnostic missing~~ — covered by the cross-outbound `dial-domain-strategy-deprecated` warning landed under 1.12-C; new lock test in domain.test.ts ("lock-tests dial-domain-strategy-deprecated covers outbound:direct") pins coverage. |
| outbound-block | ✅ ~~No canvas deprecation badge~~ — fixed 2026-05-27, SbcNode renders an amber "deprecated" pill on the title bar and dims the card border when `kind === "outbound" && type === "block"`. ✅ `outbound-block-deprecated` diagnostic landed under 1.11-A. |
| outbound-socks | ✅ Closed 2026-05-27 — false positive. `Network` enum lives in the shared outbound block at Inspector.tsx:3188 which whitelists `socks`/`http`/`shadowsocks`/`vmess`/`trojan`/`vless`/`tuic`/`hysteria`/`hysteria2` and renders the `(both)` / `tcp` / `udp` select. `Username` + `Password` (SensitiveTextField) render in the `["http", "socks", "naive"].includes(entityType)` block at Inspector.tsx:3222. Both are first-class for socks outbound; the audit's claim that they were missing was incorrect. |
| outbound-http | ✅ ~~**`headers` map invisible** + `path` not in `outboundHandledFields`~~ — fixed 2026-05-27, outbound http now has a dedicated block that renders `Path` (text input, placeholder `/proxy`) and a `Headers` key-value map editor (Name/Value rows, Add/Remove buttons; same pattern as the naive extra_headers editor). Both `path` and `headers` added to `outboundHandledFields` so they don't double-render via AdvancedScalarFields/NonScalarFields and so existing values round-trip on import. Regression test "renders outbound:http path + headers map editor with round-trip persistence" locks both controls and the resulting store state. |
| outbound-shadowsocks | ✅ ~~`password` not rendered in shadowsocks block~~ — false positive in original audit; shared password block at Inspector.tsx:2730 already renders `Password` (SensitiveTextField) for shadowsocks/trojan/naive/tuic/hysteria2/anytls/shadowtls. Regression test in app.test.tsx ("renders Password as a first-class sensitive field for shadowsocks-like outbounds") locks this. ✅ `commands.ts network:"tcp"` removed under CC-3. `network` enum select absent in shadowsocks-specific sub-block — fall through to AdvancedScalarFields. `udp_over_tcp` / `multiplex` conflict diagnostic still absent. |
| outbound-vmess | ✅ ~~`network` default in `commands.ts:319` still `"tcp"`~~ — false positive / already resolved by CC-3. Current vmess scaffolds (both inbound at commands.ts:149 and outbound at commands.ts:306) carry no `network` field. Confirmed via `grep -n 'network\|tcp' src/domain/commands.ts` — no matching default in either vmess branch. |
| outbound-trojan | ✅ ~~`password` not in `outboundHandledFields`~~ — false positive; `password` is at outboundHandledFields[180], and the shared shadowsocks-like password block at Inspector.tsx:2940 renders it as SensitiveTextField for trojan. Covered by the regression test "renders Password as a first-class sensitive field for shadowsocks-like outbounds". ✅ `network` default removed under CC-3. |
| outbound-naive | ✅ Fully closed 2026-05-27. ~~`username` no dedicated branch (degrades to Advanced)~~ — false positive; outbound block at Inspector.tsx:3236 whitelists `["http", "socks", "naive"]` and renders `Username` + `Password` for all three (Password is conditional on `entityType !== "naive"` per the docs which classify naive's auth differently). ✅ ~~`quic_congestion_control` enum select missing~~ — fixed earlier, outbound naive block now renders a `QUIC Congestion Control` `<select>` with the 4 algorithms documented at `outbound/naive.md` (`bbr` / `bbr2` / `cubic` / `reno`) plus `(default — bbr)`. `quic_congestion_control` added to `outboundHandledFields`. Regression test in app.test.tsx ("renders outbound:naive quic_congestion_control as an enum select with bbr/bbr2/cubic/reno") locks the control type + option set + write-through. |
| outbound-hysteria | ✅ Closed 2026-05-27. ~~`up_mbps` / `down_mbps` not in `outboundHandledFields`~~ — false positive; both keys already present at Inspector.tsx:223-224. The actual gap was UI: hysteria v1 block only rendered Auth (string). Added `Up Mbps` + `Down Mbps` numeric inputs (empty = no rate limit) and an `Obfs (string, optional)` text input directly to the `entityType === "hysteria"` branch. ✅ ~~`hysteria-v1-deprecated` diagnostic missing~~ — fixed earlier. `outbound-missing-tls` already covers TLS-required. Regression test "renders Up/Down Mbps + obfs controls on outbound:hysteria (v1)" locks the controls and write-through (numeric coercion + string passthrough). |
| outbound-hysteria2 | ✅ ~~`up_mbps`/`down_mbps` not surfaced in outbound~~ — fixed 2026-05-27, both fields rendered as first-class number inputs in the hysteria2 outbound block with a helpful "empty = let BBR pick" placeholder. Added to `outboundHandledFields`. ✅ `outbound-missing-tls` (general TLS-required diagnostic) covers hysteria2. |
| outbound-tuic | ✅ ~~uuid + password not first-class~~ — false positive in original audit; UUID lives in the shared vmess/vless/tuic UUID block at Inspector.tsx:2691 (SensitiveTextField + Generate UUID button), Password in the shared shadowsocks-like block at line 2730. Regression test in app.test.tsx ("renders UUID + Password as first-class fields with Generate UUID button on tuic outbound") locks both. ✅ scaffold network removed under CC-3. ✅ ~~`heartbeat`, `zero_rtt_handshake` still degrade to Advanced~~ — fixed 2026-05-27, outbound tuic block now renders a `Heartbeat` text field (placeholder `10s`, matching the documented default) and a `0-RTT Handshake` toggle row directly after `UDP over Stream`. Both keys added to `outboundHandledFields` so they don't double-render via AdvancedScalarFields. Regression test "renders outbound:tuic heartbeat + zero_rtt_handshake as first-class controls" locks placeholder, write-through, and store state. |
| outbound-vless | ✅ Clean. |
| outbound-shadowtls | ✅ ~~`version` no enum select~~ — fixed 2026-05-27, outbound shadowtls block now renders a `Version` `<select>` with 3 labeled options. No version-conditional password visibility. No version-conditional warning diagnostic. |
| outbound-anytls | ✅ ~~`idle_session_*` missing~~ — fixed 2026-05-27, anytls outbound now has an "Idle session" fieldset with three first-class controls (check interval, timeout, min sessions). Added to `outboundHandledFields`. ✅ 1.12+ version gate is N/A. |
| outbound-ssh | ✅ ~~Three-way auth mutual exclusion has no diagnostic~~ — fixed 2026-05-27, `ssh-auth-mutex` warning fires when more than one of `password` / `private_key` / `private_key_path` is set. ✅ ~~1.14 testing fields (`cipher`, `mac`, `kex_algorithm`) have diagnostics but no editing controls~~ — fixed 2026-05-27, outbound ssh block now renders three CSV inputs (`Ciphers (CSV)` / `MACs (CSV)` / `Key Exchange Algorithms (CSV)`) preceded by a `version` PlatformBanner that explicitly notes the three fields only take effect on sing-box 1.14+ testing. All three keys added to `outboundHandledFields`. Regression test "renders outbound:ssh cipher/mac/kex_algorithm CSV inputs (1.14)" locks the parsed arrays end-to-end. |
| outbound-tor | ✅ Clean. |
| outbound-selector | ✅ ~~`compatible` array limited to `["SOCKS","Direct","Block"]`~~ — fixed 2026-05-27; expanded to all 19 outbound types (Direct/Block/SOCKS/HTTP/Shadowsocks/VMess/Trojan/Naive/Hysteria/Hysteria2/ShadowTLS/VLESS/TUIC/AnyTLS/Tor/SSH/WireGuard/Selector/URLTest). |
| outbound-urltest | ✅ ~~`createOutbound("urltest")` scaffold doesn't seed `tolerance` / `idle_timeout` / `interrupt_exist_connections`~~ — fixed 2026-05-27, scaffold now seeds the three documented defaults explicitly (`tolerance: 50`, `idle_timeout: "30m"`, `interrupt_exist_connections: false`) so the UI's first-class controls don't render as blank placeholders for the defaults that sing-box already silently uses. Regression test "seeds tolerance/idle_timeout/interrupt_exist_connections defaults on urltest scaffold" locks the shape. |

### DNS server

| Node | Defect |
| --- | --- |
| dns-server-local | ✅ Closed 2026-05-27. ~~`prefer_go` not in `dnsServerHandledFields`~~ fixed — added `prefer_go` to the Set and rendered a first-class toggle row inside the new `entityType === "local"` block in Inspector. Label includes "since sing-box 1.13.0" so users see the channel requirement inline. ~~`neighbor_domain` has no "Since 1.14.0 (testing)" annotation nor channel-gate diagnostic~~ fixed — new `dns-server-neighbor-domain-testing-only` diagnostic now fires on `channel === "stable"` whenever a `dns.servers[]` entry of type `local` has a non-empty `neighbor_domain[]`, mirroring the existing `ssh-cipher-testing-only` pattern. Regression tests "renders prefer_go toggle on dns-server:local with a 1.13+ note" + "emits dns-server-neighbor-domain-testing-only on stable only" lock both. |
| dns-server-hosts | ✅ ~~`path` strongly typed as `string`~~ — fixed; multi-path support landed. ✅ ~~No `predefined` non-empty diagnostic~~ — fixed 2026-05-27, `dns-server-hosts-empty` warning fires when neither `predefined` (non-empty map) nor `path` (non-empty string/array) is set. Canvas detour port still renders for hosts despite `dnsServerDialTypes` excluding it. |
| dns-server-udp | ✅ Clean. |
| dns-server-tcp | ✅ ~~`missing-dns-server-address` diagnostic missing~~ — fixed 2026-05-27, `dns-server-missing-server` error fires for any udp/tcp/tls/https/quic/h3 DNS server that lacks a non-empty `server` string. |
| dns-server-tls | ✅ Closed 2026-05-27. ~~`certificate_provider` still rendered as free text~~ fixed — `sharedFieldDefinitions(group === "tls", …)` now builds `certificateProviderOptions` from `config.certificate_providers[].tag` and emits the TLS card's Certificate Provider field as `kind: "select"` (with leading empty option). All TLS-bearing entities (dns-server-tls/-h3/-quic/-https, all inbound trojan/naive/hysteria/hysteria2/tuic/anytls, etc.) share this change. Regression test "renders TLS certificate_provider as a select sourced from config.certificate_providers tags" locks the dropdown values via a trojan inbound. ✅ ~~`server` field has no required indicator/diagnostic~~ — fixed earlier (covered by the new `dns-server-missing-server` error). |
| dns-server-https | ✅ ~~Scaffold writes non-existent field `address: "https://1.1.1.1/dns-query"`~~ — fixed 2026-05-27, `commands.ts` `createDnsServer("https")` no longer emits the deprecated `address`. `DnsServerConfig.headers` field still missing from `types.ts:38-45`. No `missing-server` diagnostic. |
| dns-server-quic | ✅ Clean. |
| dns-server-h3 | Palette kind still `dns-http3` (CC-4). `DnsServerConfig.headers` missing in types. No `domain_resolver`-required-for-domain diagnostic. |
| dns-server-dhcp | ✅ Closed 2026-05-27. New `dns-server-dhcp-interface-empty` warning fires when a dns-server of type `dhcp` carries an explicit empty-string `interface` — the docs (`dns/server/dhcp.md`) state "default interface will be used by default" when the field is omitted, so an empty string is meaningless and likely a UI-induced bug. Two regression tests in domain.test.ts: one asserts the code fires for `interface: ""`, one asserts it stays absent for both `interface` omitted and `interface: "eth0"`. |
| dns-server-fakeip | ✅ ~~Legacy `dns-fakeip` Palette entry has no deprecation badge~~ — already in `deprecatedKinds` set so itemStatus returns "deprecated" and the button title reads "deprecated by sing-box; new configs should use the recommended replacement". New lock test in app.test.tsx ("marks the legacy top-level FakeIP palette item as deprecated"). Canvas subtitle stays generic. Two Palette entries still share the same icon. |
| dns-server-resolved | ✅ ~~`service` field has no Inspector control~~ — fixed 2026-05-27. Inspector resolved branch now renders a `Service` select (limited to `service:resolved` tags) plus an `accept_default_resolvers` toggle. `graph.ts` emits `edge:dns-server-service:<dns>:<service>` and SbcNode exposes matching ports (dns-server output + service:resolved input). Two new diagnostics: `dns-server-resolved-service-missing` (warning) and `dns-server-resolved-service-not-found` (error). |
| dns-server-tailscale | ✅ ~~`accept_default_resolvers` not in `dnsServerHandledFields`~~ — fixed 2026-05-27; rendered as a first-class toggle in the tailscale Inspector branch and added to `dnsServerHandledFields`. ✅ scaffold no longer seeds the dangling `endpoint`. `SbcNode.tsx` always renders outbound detour port; tailscale should suppress it. `accept_search_domain` (testing 1.14) has diagnostic but no UI. |

### Endpoint

| Node | Defect |
| --- | --- |
| endpoint-wireguard | ✅ ~~`peer.public_key` uses plain `<input>` — no sensitive masking~~ — fixed 2026-05-27; peer.public_key now wrapped in SensitiveTextField (matches peer.pre_shared_key). `address` is CSV via `toList`/`fromList`, no CIDR-tagged repeater. No wireguard-specific diagnostics (private_key / peers / peer.allowed_ips emptiness). `listen_port` / `workers` not in `endpointHandledFields`. |
| endpoint-tailscale | ✅ Mostly closed 2026-05-27. ~~`auth_key` not wrapped in `SensitiveTextField` inside the tailscale block~~ fixed — the block now opens with a SensitiveTextField(`Auth Key`, placeholder `tskey-auth-...`) wired to `auth_key`. ~~`advertise_tags` and `system_interface` have no 1.13 channel gate~~ fixed — added two new warnings under the `channel === "stable"` block: `endpoint-tailscale-advertise-tags-1-13-only` and `endpoint-tailscale-system-interface-1-13-only` (message explicitly calls out "Stable 1.12 targets reject it"). Both keys added to `endpointHandledFields` so they don't double-render via the Advanced section. The remaining gap — 14 fine-grained tailscale knobs (`ephemeral`, `hostname`, `exit_node`, `exit_node_allow_lan_access`, etc.) — stays in AdvancedScalarFields since they're hosting/quotas-style settings rarely touched in a sing-box GUI; promoting them all would dwarf the rest of the inspector. Regression tests: "renders tailscale endpoint auth_key as a sensitive field + system_interface input" (Inspector) and "emits endpoint-tailscale-advertise-tags-1-13-only + system-interface gate on testing channel" (diagnostics, despite the name it runs on stable channel — name kept for grep symmetry with the existing 1.14 codes). |

### Service

| Node | Defect |
| --- | --- |
| service-ssm-api | ✅ Closed 2026-05-27. The `connectDirectedPortReference` handler for the `inbound:shadowsocks → service:ssm-api` edge (useProjectStore.ts:384) now writes the inbound's `managed: true` flag in addition to wiring the service's `servers["/"]` reference. This is the canvas equivalent of a UI auto-fix — the user no longer has to hand-edit `managed: true` in the inbound Inspector after drawing the link. The existing `ssm-api-managed-mismatch` diagnostic still catches old configs imported with a missing flag. Regression test "auto-sets shadowsocks inbound.managed=true when connecting to an ssm-api service" calls `connectPorts` end-to-end and asserts both the inbound flag and the service map. |
| service-derp | ✅ ~~scaffold returns 8 fields with no `tls`~~ — fixed 2026-05-27. ✅ ~~Default `listen: "127.0.0.1"` inappropriate~~ — fixed 2026-05-27, scaffold now listens on `"::"` (all IPv4 + IPv6). `derp-service-needs-tls` diagnostic still fires for any imported derp without TLS. Inspector has no inline TLS-required banner. `config_path` still no required marker. `verify_client_url` / `mesh_with` / `stun` are `JsonField` only. |
| service-hysteria-realm | ✅ Closed 2026-05-27. ~~No users-required diagnostic + no name/token diagnostics + no placeholder-token warning~~ fixed — added four new codes: `hysteria-realm-users-required` (error, empty users[]), `hysteria-realm-user-name-required` (error, missing/blank name), `hysteria-realm-user-token-required` (error, missing/blank token), and `hysteria-realm-user-token-placeholder` (warning, token is the scaffold default `"change-me"`). Together they cover all the foot-guns the audit row called out. Three regression tests in domain.test.ts (empty users, change-me token, blank token) lock the three primary paths. The Palette statusTitle gap is cosmetic — the existing `hysteria-realm-testing-only` diagnostic already surfaces the channel restriction in the diagnostics panel. |
| service-ccm | ✅ Mostly closed 2026-05-27. ~~`credential_path` / `usages_path` scaffold writes empty strings~~ fixed — ccm scaffold now omits both keys (and `headers: {}`); empty `users: []` stays since the diagnostic now demands it be populated. ~~No ccm-specific diagnostics (public listen warning, empty users)~~ fixed — added two new warnings: `ccm-users-empty` (fires when `service.users[]` is empty or missing — remote clients would be rejected) and `ccm-public-listen` (fires when `listen` is `0.0.0.0` / `::` / `""` — recommends 127.0.0.1 bind). `missing-service-detour` already covers the detour gap (diagnostics.ts:124, type-agnostic). Two regression tests in domain.test.ts lock positive + negative cases. Canvas subtitle staying static is intentional — the entity-summary helpers prefer stable strings to avoid every-frame churn. |
| service-ocm | ✅ Clean. |
| service-resolved | ✅ ~~`SbcNode.tsx` returns empty port array for resolved~~ — fixed 2026-05-27, service:resolved now has a `dns-server` input port. Canvas edge from dns-server:resolved to service:resolved emitted by graph.ts. `DnsServerConfig.path` widened earlier; `service` is read via the broader `[key: string]: unknown` escape but a regression test verifies edge generation. Store-side `dns-server-service → service:resolved` connect handler still pending. |

## Suggested Repair Plan

Order this work to maximise nodes-closed-per-PR.

1. **CC-1 — gate `address` / `auto_route` on tun** — touches `Inspector.tsx:2268-2282` only; closes a P0 on ~15 inbound nodes.
2. **CC-2 — first-class controls for `password` / `uuid` / `network` / `users` across affected protocols** — biggest single chunk; ~12 nodes.
3. **CC-3 — strip `network: "tcp"` / `"udp"` defaults from scaffolds** — touches `commands.ts` only; behavioural fix on ~5 outbound nodes.
4. **CC-4 — Palette kind rename `mixed → inbound-mixed`, `dns-http3 → dns-h3`** — touches `Palette.tsx` + tests; closes naming P0.
5. **CC-5 — TLS-required / users-required diagnostics for trojan / tuic / hysteria / hysteria2 / naive / derp** — `diagnostics.ts` only.
6. **CC-6 — action-gate canvas ports in `graph.ts`** for route-rule + dns-rule.
7. **CC-7 — Palette badge surfacing** for redirect / tproxy / hysteria-realm gates.
8. Then split node-specific P0s into per-family atomics: settings (4), hub (1), rule (2), inbound (~10), outbound (~12), dns-server (~8), endpoint (2), service (4).

## Method

- 7 batches × 10 / 10 / 10 / 10 / 10 / 10 / 6 = 66 agents, each running in parallel within a batch.
- Each agent: read `docs/ui-reviews/<node>.md` + `docs/claude/<node>.md` to extract claimed P0/P1 findings only (not to trust them), then grep `src/components/Inspector.tsx`, `src/components/Palette.tsx`, `src/components/SbcNode.tsx`, `src/canvas/graph.ts`, `src/domain/commands.ts`, `src/domain/diagnostics.ts`, `src/domain/sharedFieldRegistry.ts`, `src/domain/types.ts`, `src/domain/protocols.ts`, `src/state/useProjectStore.ts`.
- Each agent reported only deltas; "all good" findings collapsed to "remaining N already landed".
- Five nodes returned zero deltas: `outbound-vless`, `outbound-tor`, `dns-server-quic`, `dns-server-udp`, `service-ocm`.

When a delta is closed in code, drop the row from this doc and update the matching `docs/ui-reviews/<node>.md` + `docs/claude/<node>.md` status comment.
